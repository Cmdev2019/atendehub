# 🗺️ Roadmap de Correção e Estabilização — AtendeHub

> **📌 DOCUMENTO VIVO** — Este é o documento canônico de status do projeto.
> Ele é atualizado em tempo real conforme o trabalho avança. Toda sessão de
> trabalho (humana ou com Claude) deve atualizar os status dos itens, o painel
> de progresso e registrar uma entrada no [Changelog](#-changelog).
>
> **Última atualização:** 2026-07-26 · **Fases 0-8 seguem 70/70 concluídas**
> — nenhuma auditoria desta sessão ou da anterior invalidou item já fechado,
> só achou lacunas novas fora do escopo original das fases. **Backlog em
> 31 itens (B-1 a B-31)** — B-31 é a 1ª entrada pós-backlog-fechado: demanda
> de produto nova pedida pelo usuário (etapas de atendimento), não uma
> lacuna descoberta em auditoria. **Todo o trabalho de B-7 a B-24 (49
> arquivos, 12 itens) estava concluído e validado mas não commitado** até
> uma sessão anterior — commit `a9d9973` fechou isso; working tree limpo.
> **🏁 Backlog original (B-1 a B-30) 100% fechado**, todos concluídos ou
> decididos conscientemente — CI real no GitHub Actions (B-16), upgrade
> NestJS 10→11 (B-25), reset administrativo de senha (B-26), UI de tags
> (B-27), o pacote de retenção de dados/LGPD (B-17/B-28/B-29/B-30),
> monitoramento de erros com Sentry validado com DSN real (B-18), e B-19
> decidido (manter `localStorage`). **Depois disso:** revisão de código
> completa do frontend (2 bugs reais corrigidos + 3 arquivos mortos
> removidos), QA visual completo sem erros, checklist de deploy (imagens
> de produção buildadas e testadas contra Postgres/Redis reais — falta
> certificado SSL/domínio pra deploy real, que ainda não existem), e então
> **B-31**: etapas de atendimento no frontend (Fila de atendimento → Meus
> → Em atendimento → Encerrados) — o backend já tinha o pipeline pronto,
> só faltava o frontend consumir. **2026-07-26:** correção pedida pelo
> usuário sobre o próprio B-31 — o seletor de etapas em 4 botões quebrava o
> layout da coluna da fila ("ficou estranho"), e as 3 abas ativas
> dividiam uma única paginação genérica (sem filtro), imprecisa em relação
> à quantidade real de conversas de cada etapa. Trocado por um dropdown
> único (`<select>`, sempre 1 linha) e por paginação própria por aba
> (`loadMoreQueueConversations`/`loadMoreMineConversations`/
> `loadMoreOpenConversations`, cada uma com seu filtro `status`/`agentId`
> no servidor, igual ao que "Encerrados" já fazia). Ver Changelog para o
> detalhe. **2026-07-26: nova leva de produto pedida pelo usuário, registrada
> como B-32 a B-35** — Dashboard (substitui "Funis", vira 1ª tela antes da
> Caixa de Entrada), Relatórios (base de atendimento/tipos/por atendente),
> Contatos (CRUD no frontend, backend já pronto) e Auto-atendimento
> (saudação/menu/inatividade/encerramento/horário, configurável em
> Configurações). Ordem de execução definida pelo usuário: B-32 → B-33 →
> B-34 → B-35. **B-32 concluído no mesmo dia:** dashboard real (dados
> agregados via Prisma `count`, `recharts` pra visualização), motivo do
> encerramento (resolvido/não resolvido) virou pergunta obrigatória no botão
> "Encerrar" — decisão do usuário entre 3 opções apresentadas. **B-33
> concluído no mesmo dia:** 3 relatórios (base de atendimento, por tipo/tag,
> por atendente) com exportação CSV/PDF (`PDFKit`, novo) — "Relatórios"
> deixou de ser "em breve" no menu. **Próxima ação:** B-34 (base de
> contatos no frontend — backend já tem CRUD completo, é trabalho
> majoritariamente de UI).

---

## 📊 Painel de status

| Fase | Objetivo | Prioridade | Progresso | Status |
|---|---|---|---|---|
| [Fase 0](#-fase-0--religar-frontend--backend) | Religar frontend ↔ backend | 🔴 P0 | 13/13 | ✅ Concluída |
| [Fase 1](#-fase-1--contrato-de-dados-único) | Contrato de dados único | 🔴 P1 | 11/11 | ✅ Concluída |
| [Fase 2](#-fase-2--modo-demo-explícito-e-resiliência) | Modo demo explícito e resiliência | 🟠 P1 | 8/8 | ✅ Concluída |
| [Fase 3](#-fase-3--ativar-o-módulo-de-sla) | Ativar o módulo de SLA | 🟠 P1 | 5/5 | ✅ Concluída |
| [Fase 4](#-fase-4--testes-no-backend) | Testes no backend | 🟠 P1→P2 | 6/6 | ✅ Concluída (via B4, `ROADMAP_BACKEND.md`) |
| [Fase 5](#-fase-5--higiene-do-repositório-e-documentação) | Higiene do repo e documentação | 🟡 P2 | 5/5 | ✅ Concluída |
| [Fase 6](#-fase-6--hardening-de-segurança-pré-produção) | Hardening de segurança | 🟡 P2 | 5/5 | ✅ Concluída |
| [Fase 7](#-fase-7--pronto-para-produção) | Pronto para produção | 🟢 P3 | 5/5 | ✅ Concluída |
| [Fase 8](#-fase-8--administração--configurações) | Administração & Configurações | 🟠 P1 | 12/12 | ✅ Concluída |
| **Total** | | | **70/70** | |

**Legenda de status:** ⬜ Pendente · 🔄 Em andamento · 🔍 Em validação · ✅ Concluído · ⛔ Bloqueado · 🚫 Cancelado

**Dependências entre fases:** Fase 0 destrava as Fases 1, 2 e 3. As Fases 4 e 5 podem rodar em paralelo a qualquer momento. A Fase 6 é pré-requisito de produção; a Fase 7 fecha o ciclo.

---

## 📖 Regras do documento vivo

1. **Ao iniciar um item:** mudar status para 🔄 e anotar a data de início na coluna Status.
2. **Ao concluir um item:** só marcar ✅ depois de verificar o **critério de aceite** — a evidência (comando executado, teste, screenshot) vai no Changelog.
3. **Ao final de cada sessão de trabalho:** atualizar o painel (contadores e "Próxima ação") e adicionar entrada no Changelog com data, itens tocados e evidências.
4. **Problema novo descoberto:** vira item com ID na fase adequada (ou no [Backlog](#-backlog)) **antes** de ser corrigido — nada de correção sem registro.
5. **Decisão de arquitetura/produto:** registrar na seção [Decisões](#-registro-de-decisões) com data e motivo.
6. **Nunca** remover itens — cancelados viram 🚫 com justificativa.

---

## 🔴 Fase 0 — Religar frontend ↔ backend

**Problema:** hoje a integração está quebrada em múltiplos pontos independentes — mesmo com a API no ar, o frontend opera 100% em mock sem avisar.
**Meta:** com `docker compose up` + API + frontend rodando, o fluxo completo usa dados reais: login → conversas → envio → recebimento em tempo real.

| ID | Item | Referências | Status |
|---|---|---|---|
| F0-1 | Criar endpoint público `GET /api/v1/health` no NestJS (novo `HealthModule`). Hoje `testConnection()` consulta essa rota, recebe 404 e rebaixa **todas** as chamadas para mock, mesmo com a API rodando. **Aceite:** `curl http://localhost:3001/api/v1/health` → 200 `{ status: "ok" }`; frontend loga backend disponível e para de usar mock. | `src/services/api.js:19-30` (consumidor) · `apps/api/src/modules/health/` (novo) | ✅ 2026-07-15 |
| F0-2 | Conectar o socket no namespace `/ws`. O gateway escuta em `/ws`, mas o cliente conecta na raiz. Ajustar a URL de conexão e `VITE_WS_URL` no `.env.example` para `http://localhost:3001/ws`. **Aceite:** log "cliente conectado" no gateway + `wsClient.isConnected === true` no navegador. | `apps/api/src/modules/events/events.gateway.ts:36` · `src/services/websocket.js:3,17` · `.env.example:7` | ✅ 2026-07-15 |
| F0-3 | Alinhar eventos servidor→cliente. O backend emite `message.new`, `message.status`, `conversation.created`, `conversation.updated`, `conversation.assigned`, `connection.status`, `sla.breached` (com ponto). O frontend escuta `message:created` e `conversation:updated` (dois-pontos, nomes diferentes) — nenhum evento é tratado. Padronizar no formato do backend. **Aceite:** mensagem recebida via webhook aparece na UI sem refresh. **Bônus:** emissões do backend unificadas em união de salas (fim do evento duplicado para sockets em 2 salas). | `apps/api/src/modules/events/events.service.ts` · `src/services/websocket.js` · `src/hooks/useConversations.js` | ✅ 2026-07-15 |
| F0-4 | Corrigir envio de mensagem (2 defeitos): **(a)** o cliente emite `send:message` via socket, mas o gateway não tem handler para isso (só `join:conversation`, `leave:conversation`, `ping`) — a mensagem some; remover o caminho socket e enviar sempre via REST. **(b)** o REST envia `{ text }`, mas `SendMessageDto` exige `{ type, content }` e o `ValidationPipe` com `forbidNonWhitelisted` rejeita `text` com 400. **Aceite:** mensagem enviada pelo front persiste no banco e entra na fila de envio sem erro. **Validado 2026-07-16:** round-trip real na madrugada — respostas enviadas pelo painel persistidas com status `DELIVERED` (entrega confirmada pelo WhatsApp). | `src/hooks/useConversations.js` · `src/services/api.js:193-200` · `apps/api/src/modules/message/dto/send-message.dto.ts:24-36` | ✅ 2026-07-16 |
| F0-5 | Entrar/sair das salas de conversa. `joinConversation()`/`leaveConversation()` existem no client mas **nunca são chamados** — sem entrar na sala `conversation:<id>`, eventos por conversa não chegam. Chamar ao selecionar/trocar conversa ativa. **Aceite:** eventos da conversa ativa chegam ao cliente (verificável via log do gateway). | `src/services/websocket.js` · `src/hooks/useConversations.js` · `apps/api/src/modules/events/events.gateway.ts:162,196` | ✅ 2026-07-15 |
| F0-6 | Validação E2E do fluxo real: `docker compose up` → seed → login → conversas reais na fila → enviar mensagem → receber evento em tempo real. Registrar evidências no Changelog. **Aceite:** fluxo completo sem fallback para mock. **Concluído 2026-07-16:** fluxo completo validado com WhatsApp real — mensagens de cliente recebidas em tempo real + respostas do painel entregues no celular (`AGENT`/`DELIVERED` no banco, 01:44–02:05). Obs. histórica: a senha do admin do seed era aleatória; realinhada para `Admin@123` em F2-7. | `README.md` (passos) | ✅ 2026-07-16 |
| F0-7 | **[Descoberto na execução]** `npm run db:seed` estava quebrado por 2 defeitos: (a) ts-node não carrega `.env` → `Environment variable not found: DATABASE_URL`; (b) `setTimeout` de 1h (auto-delete do arquivo de credenciais) prendia o processo — o comando nunca terminava. Corrigido: `-r dotenv/config` no script + remoção do auto-delete (o arquivo já é gitignored). | `apps/api/package.json` (`db:seed`) · `apps/api/prisma/seed.ts:130-133` | ✅ 2026-07-15 |
| F0-8 | **[Descoberto na execução]** `npm run start` quebrado: o build incluía `prisma/**` e gerava `dist/src/main.js` em vez de `dist/main.js` (`Cannot find module dist/main`). Criado `tsconfig.build.json` compilando apenas `src/` (convenção NestJS). | `apps/api/tsconfig.build.json` (novo) · `apps/api/tsconfig.json:24` | ✅ 2026-07-15 |
| F0-9 | **[Descoberto no smoke test]** Evolution API em **crash-loop desde sempre** neste ambiente: o compose apontava `DATABASE_CONNECTION_URI` para o MESMO banco `atendehub` da API — ambos usam Prisma e colidem na `_prisma_migrations` (`relation "_prisma_migrations" already exists`). Havia ainda um container órfão de um projeto compose antigo reiniciando em loop e bloqueando o nome `atendehub_evolution`. Corrigido: banco dedicado `evolution` criado, compose e `init.sql` atualizados, container órfão removido. | `docker-compose.yml` · `infra/postgres/init.sql` | ✅ 2026-07-15 |
| F0-10 | **[Descoberto no round-trip real]** Webhooks de mensagem rejeitados com **413 Payload Too Large**: a Evolution envia mídia embutida em base64 (`webhookBase64: true`) e o corpo estourava o limite padrão de ~100kb do Express — mensagens reais chegavam na Evolution e se perdiam na API. Corrigido: body parser manual com limite de 25mb **apenas** na rota `/api/v1/webhooks` (1mb no resto da API). | `apps/api/src/main.ts` | ✅ 2026-07-15 |
| F0-11 | **[Descoberto no round-trip real]** Nomes de contato corrompidos por 2 defeitos no processamento do webhook: (a) JID com sufixo de dispositivo (`5512...:12@s.whatsapp.net`) não era normalizado → o mesmo número virava contatos duplicados; (b) em mensagens `fromMe`, o `pushName` é o **dono da conexão** — e o upsert renomeava o contato para o nome do dono a cada resposta enviada pelo celular. Corrigido: `split(':')[0]` na extração do número (tb. no handler de CONTACTS_UPSERT) e `pushName` ignorado quando `fromMe`; `upsertFromWebhook` aceita nome opcional com fallback para o telefone na criação. Dados de teste duplicados removidos do banco. | `apps/api/src/modules/webhook/webhook.service.ts` · `apps/api/src/modules/contact/contact.service.ts` | ✅ 2026-07-15 |
| F0-12 | **[Reportado pelo usuário 2026-07-21 — "conversas duplicadas"]** `ConversationService.upsertFromWebhook` reaproveitava conversa `WAITING`/`OPEN` filtrando também por `whatsappConnectionId`. Toda vez que a conexão WhatsApp era apagada e recriada (aconteceu várias vezes na história do projeto — F8-9, F8-10, F8-12), o `id` da conexão mudava e a query de "conversa já aberta" deixava de casar: o contato ganhava uma conversa **nova**, e a antiga virava órfã (`whatsappConnectionId` vira `NULL` quando a conexão referenciada é apagada) — presa para sempre na fila, sem poder responder (`SendMessageService` rejeita com "conversa não tem conexão associada"). 5 contatos com 2-3 conversas simultâneas encontrados no banco. Corrigido: reaproveitamento agora casa só por `companyId + contactId + status`, e REAPONTA `whatsappConnectionId` para a conexão atual quando encontra uma conversa existente presa numa conexão antiga (self-healing na próxima mensagem do contato, sem precisar de faxina manual). **Dados existentes:** 6 conversas órfãs (as mais antigas de cada contato duplicado) fechadas (`status=CLOSED`) diretamente no banco — nenhuma tinha a conexão ativa, então fechar a mais antiga e manter a mais recente não perde nenhuma conversa "viva". **Efeito colateral descoberto junto:** `GET /conversations` sem filtro de `status` retornava TODAS (inclusive `CLOSED`) — as 6 fechadas continuariam aparecendo na Caixa de Entrada. Corrigido: sem `status` explícito, a listagem agora exclui `CLOSED` por padrão (quem quiser ver encerradas pede `?status=CLOSED`). | `apps/api/src/modules/conversation/conversation.service.ts` (`upsertFromWebhook`, `findAll`) | ✅ 2026-07-21 |
| F0-13 | **[Descoberto investigando F0-12, ao vivo com tráfego real]** JID `@lid` (identificador de privacidade do WhatsApp — usado quando o remetente esconde o número real) não era reconhecido: a extração de telefone só tratava `@s.whatsapp.net`/`@c.us` via `.replace()`; um `@lid` passava direto sem normalizar, criando contato com nome/telefone literalmente `"179542585000066@lid"`. Corrigido: extração generalizada por domínio (`jid.split('@')[0].split(':')[0]`, mesmo padrão já usado em `evolution.service.ts`) em vez de enumerar sufixos — cobre `@lid` e qualquer domínio futuro sem precisar de nova lista; log de aviso quando um `@lid` é recebido (o número por trás dele fica genuinamente oculto pelo WhatsApp, não é bug nosso recuperá-lo). Registro já corrompido no banco corrigido para o id normalizado. **Pendente/observação:** não há como confirmar sem tráfego ao vivo se a Evolution expõe o número real por outro campo do payload para remetentes `@lid — não investigado mais a fundo por falta de payload bruto para inspecionar (`webhooks` não loga corpo recebido). | `apps/api/src/modules/webhook/webhook.service.ts` (`extractPhoneFromJid`) | ✅ 2026-07-21 |

---

## 🔴 Fase 1 — Contrato de dados único

**Problema:** o mock usa um shape (`type: 'agent'`, `text`, `time: 'HH:MM'`) diferente do modelo real da API (`senderType`, `content`, `createdAt`). Quando a Fase 0 religar os dados reais, os componentes tendem a quebrar.
**Meta:** um único contrato (o da API) usado por componentes, mock e testes.

| ID | Item | Referências | Status |
|---|---|---|---|
| F1-1 | Documentar o contrato real de resposta da API (auth, conversations, messages, contacts) a partir dos DTOs/services do backend — incluir paginação (`response.data`?) e nomes de campos. Salvar em `docs/API_CONTRACT.md`. **Feito:** documento completo com os 3 formatos de listagem (`{data,meta}` offset, `{data,meta}` cursor e array puro), 9 enums, todos os 14 recursos REST (incl. `messages/media` e webhooks), contrato Socket.IO (`/ws`, salas, 9 eventos com payloads) e a lista de divergências conhecidas do mock (alimenta F1-2/F1-3). | `docs/API_CONTRACT.md` (novo) | ✅ 2026-07-16 |
| F1-2 | Adaptar os componentes ao shape real (ou criar camada normalizadora única no service). Componentes afetados: ChatPanel, ConversationQueue, CustomerPanel, Metrics. **Aceite:** UI renderiza dados reais do seed sem erro no console. **Parcial 2026-07-15:** normalizadores `toUiConversation`/`toUiMessage` no hook + carregamento de mensagens sob demanda (`getMessages`) — sem isso o dashboard quebrava com dados reais (`conv.contact.toLowerCase` em objeto). **Parcial 2026-07-17:** evento `conversation.created` tratado — conversa nova entra no topo da fila em tempo real, com rede de segurança (`message.new` de conversa desconhecida busca via `GET /conversations/:id`; antes era descartado em silêncio) e preview da fila atualizado a cada mensagem do cliente. **Concluído 2026-07-21:** `Metrics` deixou de ser 4 strings fixas no código (`'34'`, `'2m 41s'`, `'92%'`...) e passou a calcular indicadores reais a partir da lista de conversas carregada (total, `OPEN`, `WAITING`, soma de `unreadCount`); "Primeira resposta"/"Satisfação" removidos por não terem fonte de dado nenhuma no produto hoje (sem CSAT, sem agregação de tempo de resposta — ver B-2). **[Descoberto na revisão]** o componente `Metrics` era importado em `main.js` mas **nunca era renderizado** — dead import, dashboard nunca mostrou métrica nenhuma, real ou mock; agora montado acima da fila (`.workspace-inbox`), com CSS que também não existia (`.metrics`/`.metric-card` sem nenhuma regra). | `src/components/Metrics.jsx` · `src/main.js` · `src/styles.css` · `src/hooks/useConversations.js` | ✅ 2026-07-21 |
| F1-3 | Reescrever `mockConversations.js` no shape exato da API — o mock vira espelho fiel do contrato (inclusive enums `ConversationStatus`, `SenderType`, `MessageType`). **Concluído 2026-07-21:** fixtures reescritas com `contact`/`agent`/`department`/`tags` como objetos e `status`/`channel` em enum, mensagens embutidas no shape real de mensagem (`senderType`/`content`/`sentAt`); `toUiConversation` passou a rodar também sobre o estado inicial (antes só normalizava dados vindos da API, o mock era UI-shape à mão) e decide `messagesLoaded` pela presença do array `messages` na fonte. Campos mortos do mock antigo (`status` em PT-BR, `badge`, `tone`, `value`, `initials` — nenhum lido por componente algum) eliminados. **[Bug descoberto ao rodar o mock pelo shape real]** `requestMock` roteava **qualquer** `GET /conversations/:id` e `GET /conversations/:id/messages` para `mockApiClient.getConversations()` (a listagem inteira, shape errado) — inofensivo até então porque o mock antigo nunca disparava carregamento sob demanda; passaria a quebrar silenciosamente assim que o mock virasse shape real. Corrigido: `apiMock.js` ganhou um store único de conversas (mesmas fixtures do estado inicial) com `getConversation`/`getMessages`/`sendMessage` reais; wrapper de `getConversations()` corrigido de `{data,pagination}` para `{data,meta}` (contrato). | `src/data/mockConversations.js` · `src/services/apiMock.js` · `src/services/api.js` (`requestMock`) | ✅ 2026-07-21 |
| F1-4 | `apiClient.register()` chama `POST /auth/register`, rota que **não existe** no backend (só `login`, `refresh`, `logout`, `revoke`, `me`). Decidir: remover do front ou implementar no back. Registrar em Decisões. **Decisão: removido do front** — nenhuma tela chamava `register()` (nem existe formulário de cadastro na UI); era código morto alcançável só pela metade, exposto via `AuthContext` sem nenhum consumidor. | `src/services/api.js` · `src/context/AuthContext.jsx` | ✅ 2026-07-21 |
| F1-5 | Corrigir tratamento de erro do `ApiClient`: o `request()` lança objeto literal (`throw { status, message, error }`); no catch, `error.message` pode ser `undefined` e `error.message.includes(...)` explode com TypeError, mascarando o erro original. Usar classes de erro ou checagem defensiva. **Feito junto com F2-1/F2-3:** classe `ApiError extends Error` (sempre com `.status` e `.message`); falha de rede detectada por `error instanceof TypeError` (contrato do fetch) em vez de inspecionar `message`. | `src/services/api.js` | ✅ 2026-07-16 |
| F1-6 | Atualizar os testes do frontend (66 hoje) para o novo contrato + adicionar teste do adapter/normalizador. **Aceite:** `npm test` verde. **Concluído 2026-07-21:** `toUiConversation`/`toUiMessage` exportados de `useConversations.js` + suíte dedicada (`useConversations.normalizers.test.js`, 11 casos: shape completo da API, fallback de contato sem nome, mensagens embutidas vs. lazy-load, passthrough do shape legado de string); `Metrics.test.js` novo (0 testes antes, componente nunca fora testado); `api.test.js` alinhado ao wrapper `{data,meta}` + casos de `getConversation`/`getMessages`/`sendMessage` do mock. | `src/hooks/useConversations.normalizers.test.js` (novo) · `src/components/Metrics.test.js` (novo) · `src/services/api.test.js` | ✅ 2026-07-21 |
| F1-8 | **[Reportado pelo usuário 2026-07-16]** Mensagens enviadas pelo painel **duplicadas na tela** (não no banco): corrida entre o eco via socket (`message.new`, emitido antes da resposta HTTP) e a reconciliação da mensagem otimista — o eco não casava com o id otimista e era adicionado; depois a resposta REST substituía a otimista pela mesma mensagem. Corrigido: se o eco já chegou, a otimista é apenas removida. | `src/hooks/useConversations.js` | ✅ 2026-07-16 |
| F1-9 | **[Reportado pelo usuário 2026-07-16]** Figurinhas/fotos não apareciam — 3 causas: (a) a "estratégia 1" de download baixava a URL direta do WhatsApp, que é **criptografada** (`.enc`) → bytes ilegíveis no MinIO (verificado por magic bytes); invertida a ordem — Evolution `getBase64FromMediaMessage` (decriptada) primeiro; (b) bucket MinIO sem política de leitura → navegador levava **403**; política pública de leitura aplicada no boot (produção: URLs pré-assinadas, ver F6); (c) frontend ignorava `type`/`attachments` → balão vazio; ChatPanel agora renderiza imagem/figurinha inline, áudio/vídeo com player, documento como link e placeholder ("Figurinha", "Foto"…) para mídia ainda não baixada. Anexos corrompidos antigos removidos. **Complemento (mesmo dia):** novo evento `message.updated` emitido quando o download do anexo termina — a UI troca o placeholder pela mídia real em tempo real, sem F5 (validado: figurinha nova baixada decriptada `image/webp`, URL 200). | `apps/api/src/modules/webhook/webhook.service.ts` · `apps/api/src/modules/events/events.service.ts` · `apps/api/src/shared/storage/storage.service.ts` · `src/components/ChatPanel.jsx` · `src/hooks/useConversations.js` · `src/services/websocket.js` | ✅ 2026-07-16 |
| F1-10 | **[Reportado pelo usuário 2026-07-16]** Fotos dos contatos não apareciam: o backend nunca buscava a foto de perfil (o handler de `CONTACTS_UPSERT` até salvava `profilePictureUrl`, mas o evento raramente traz isso) e o frontend só renderizava iniciais. Corrigido: novo `fetchProfilePictureUrl` no `EvolutionService` + busca fire-and-forget no processamento de mensagem quando o contato não tem avatar (throttle de 6h por contato); frontend renderiza `avatarUrl` com fallback para iniciais na fila, no cabeçalho do chat e no painel do cliente. Backfill executado: 5/6 contatos existentes com foto. | `apps/api/src/modules/whatsapp/evolution.service.ts` · `apps/api/src/modules/webhook/webhook.service.ts` · `src/components/*.jsx` | ✅ 2026-07-16 |
| F1-11 | **[Pedido do usuário 2026-07-16]** Envio de prints direto na conversa (Ctrl+V) sem salvar arquivo — e envio real dos anexos em geral (fecha o B-1 para o fluxo do chat). Backend: `POST /conversations/:id/messages/media` (multipart até 16MB) → MinIO (URL do painel) → Evolution em **base64** (o container não resolve `localhost:9000`) → persiste mensagem+attachment → `message.new` + `message.updated`. Frontend: `onPaste` no composer captura imagens da área de transferência (nomeadas `print-<ts>.png`), preview aproveitando a UI de anexos existente, envio sequencial (texto vira caption do 1º arquivo), limpeza pós-envio e detecção de FormData no `ApiClient`. | `apps/api/src/modules/message/` · `src/components/ChatPanel.jsx` · `src/hooks/useConversations.js` · `src/services/api.js` | ✅ 2026-07-16 |
| F1-7 | **[Descoberto no round-trip real]** `.chat-panel` não tinha NENHUMA regra CSS: o painel crescia junto com a conversa e empurrava o composer para fora da área visível (o `flex:1`/`overflow-y:auto` do `.chat-messages` era inerte sem pai flex). Invisível com mock (poucas mensagens); quebrou com conversa real crescendo. Corrigido: `.chat-panel` como coluna flex com `min-height:0` + `overflow:hidden` — lista rola internamente, composer fixo embaixo. | `src/styles.css` | ✅ 2026-07-15 |

---

## 🟠 Fase 2 — Modo demo explícito e resiliência

**Problema:** o fallback para mock é silencioso e vale também em produção — se a API cair, o atendente veria conversas falsas sem aviso.
**Meta:** mock só em desenvolvimento, sempre sinalizado; conexões resilientes.

| ID | Item | Referências | Status |
|---|---|---|---|
| F2-1 | Restringir fallback mock a desenvolvimento (`import.meta.env.MODE === 'development'`). Em produção: erro visível + retry, nunca dados fictícios. **Feito:** `fallbackToMock()` só roteia para o mock quando `IS_DEV`; em produção lança `ApiError` 503 ("Servidor indisponível... reconectando") e agenda revalidação de health com backoff. | `src/services/api.js` | ✅ 2026-07-16 |
| F2-2 | Banner persistente "🧪 Modo demonstração — dados fictícios" quando o mock estiver ativo. **Aceite:** banner visível em modo mock; ausente com backend real. **Feito:** novo `DemoBanner` (via `useSyncExternalStore` sobre `apiClient.onModeChange`), renderizado acima do login E do dashboard; some sozinho quando o backend volta. 3 testes cobrindo aparecer/sumir em tempo real. | `src/components/DemoBanner.jsx` (novo) · `src/main.js` · `src/styles.css` | ✅ 2026-07-16 |
| F2-3 | Eliminar a race do `testConnection()`: é async no construtor e `backendAvailable` começa `true` — as primeiras requests podem tomar a decisão errada nos dois sentidos. Aguardar o teste antes da 1ª request e revalidar com backoff (backend que voltou ao ar deve ser redetectado). **Feito:** `backendAvailable` começa `null` e `request()` aguarda `this.ready` (promise do 1º health check); health com timeout real de 3s via `AbortController` (o `timeout: 3000` antigo era no-op no fetch); backend fora do ar → recheck com backoff 5s→30s e redetecção automática (notifica o banner). | `src/services/api.js` | ✅ 2026-07-16 |
| F2-4 | Reconexão do socket após refresh de token: a conexão usa o token do momento do connect; quando o accessToken renova, reconectar com o novo token e tratar `connect_error` por expiração. **Feito:** `auth` virou função do socket.io — avaliada a CADA tentativa de (re)conexão, sempre lê o `accessToken` atual do localStorage; `connect_error` com mensagem de token ("Token inválido ou expirado" do gateway) dispara `apiClient.refreshToken()` e reconecta com o token novo (guard `refreshingToken` contra loop). | `src/services/websocket.js` | ✅ 2026-07-16 |
| F2-5 | `user:typing`: o cliente emite, mas o gateway não tem handler (no-op); o cliente também escuta `user:typing` que o servidor nunca emite. Decidir: implementar ponta a ponta ou remover do front. Registrar em Decisões. **Decisão: removido do front** (`setTyping` não tinha nenhum consumidor nos componentes). | `src/services/websocket.js` | ✅ 2026-07-16 |
| F2-6 | **[Descoberto no smoke test]** Falha de envio era **silenciosa** na UI: a mensagem otimista sumia sem aviso (o usuário tentou 3× sem feedback). Adicionado `sendError` no hook + aviso visível acima do composer (`role="alert"`), limpo ao trocar de conversa ou reenviar. Bônus: `join:conversation` agora só ocorre após conversas reais carregarem (`loadedFromApi`) — fim dos WARNs de `conv-1` a cada page load. | `src/hooks/useConversations.js` · `src/components/ChatPanel.jsx` · `src/main.js` · `src/styles.css` | ✅ 2026-07-15 |
| F2-7 | **[Pedido do usuário]** Login: mensagem única e genérica **"Usuário ou senha incorreta."** no backend real (LocalStrategy) e no mock (que diferenciava "Email não encontrado"/"Senha incorreta" — vazava existência de conta). Senha do `admin@demo.com` redefinida para a documentada no README (`Admin@123`), alinhando real = mock = documentação. Causa raiz do incidente: a API caiu e o front rebaixou o login para o mock **silenciosamente** — reforça a prioridade de F2-1/F2-2. | `apps/api/src/modules/auth/strategies/local.strategy.ts` · `src/services/apiMock.js` · `src/services/api.test.js` | ✅ 2026-07-15 |
| F2-8 | **[Descoberto pelo usuário na tela de Configurações]** Com o backend offline, todas as seções de Configurações quebravam com "⚠️ Endpoint não implementado no mock": o `requestMock` só roteava auth e conversas — `/users`, `/departments` e `/whatsapp` caíam no throw 404. Corrigido: mock ganhou estado em memória e implementações completas de users (CRUD), departments (CRUD + membros) e whatsapp (criar, QR de demonstração em SVG, status que simula pareamento após ~3 polls, desconectar, excluir); `requestMock` reescrito com roteamento por segmentos de path + método HTTP (incl. POST de mensagens → `sendMessage`, antes retornava a lista de conversas). | `src/services/apiMock.js` · `src/services/api.js:105-165` | ✅ 2026-07-15 |

---

## 🟠 Fase 3 — Ativar o módulo de SLA

**Problema:** o `SlaCheckProcessor` está bem escrito, mas é **código morto**: não está registrado em nenhum módulo NestJS e nada no código enfileira jobs na fila `sla-check`. (O `SLA_FIXES.md` corrigiu apenas a compilação, não a execução.)
**Meta:** violação de SLA detectada e alertada de ponta a ponta.

| ID | Item | Referências | Status |
|---|---|---|---|
| F3-1 | Criar `SlaModule`: registrar `SlaCheckProcessor` como provider + `BullModule.registerQueue({ name: QUEUE_NAMES.SLA_CHECK })` + importar `EventsModule`; adicionar ao `AppModule`. **Aceite:** log do Nest mostra o processor registrado no boot. **Feito via B2-2** (`ROADMAP_BACKEND.md`, 2026-07-22). | `apps/api/src/modules/sla/sla-check.processor.ts:29-30` · `apps/api/src/app.module.ts` · `apps/api/src/shared/queues/queue-names.ts:9` | ✅ 2026-07-22 |
| F3-2 | Produtor de jobs: quando uma conversa entra em `WAITING` numa `Queue` com `maxWaitSecs`, enfileirar `SlaCheckJobData` com `delay = maxWaitSecs * 1000`. Pontos prováveis: criação de conversa no webhook e mudança de status no `conversation.service`. **Feito via B2-3** (`ROADMAP_BACKEND.md`, 2026-07-22). | `apps/api/src/modules/webhook/webhook.processor.ts` · `apps/api/src/modules/conversation/conversation.service.ts` | ✅ 2026-07-22 |
| F3-3 | Idempotência e cancelamento: `jobId` determinístico por conversa (evitar jobs duplicados); confirmar que atribuição antes do prazo não gera alerta (o processor reconsulta o status — validar em teste). **Feito via B2-4** (`ROADMAP_BACKEND.md`, 2026-07-22). | `apps/api/src/modules/sla/sla-check.processor.ts:46-60` | ✅ 2026-07-22 |
| F3-4 | Frontend: ouvir `sla.breached` e destacar visualmente a conversa na fila (badge/cor). Depende de F0-2/F0-3. **Concluído:** `useConversations.js` ganhou 3 handlers novos — `sla.breached` marca `conv.slaBreached = true`; `conversation.assigned` (evento **próprio**, distinto de `conversation.updated` — o backend emite os dois separadamente em `ConversationService#assign`, e o front nunca tinha ouvido esse primeiro) limpa a flag quando um agente é atribuído (mesmo momento em que o backend cancela o job de SLA pendente); `conversation.updated` também limpa a flag quando `changes.status` sai de `WAITING` por qualquer outro caminho. `ConversationQueue.jsx` renderiza um badge "⚠ SLA" vermelho + fundo destacado no item da fila via classe `.sla-breached` nova. **Achado no caminho:** `conversation.assigned` já estava no whitelist de eventos repassados pelo `websocket.js` (`BACKEND_EVENTS`) mas **nenhum lugar do front nunca escutava**, desde sempre — descoberto ao rastrear como a UI deveria saber que uma conversa foi assumida. **Validado com Playwright real no navegador** (injeção temporária de `slaBreached:true` numa fixture do mock, revertida logo após o screenshot — sem socket real de teste disponível pra simular o breach de ponta a ponta): badge e destaque visual renderizam corretamente. | `src/hooks/useConversations.js` · `src/components/ConversationQueue.jsx` · `src/styles.css` | ✅ 2026-07-24 |
| F3-5 | Teste E2E com SLA curto (ex.: fila com `maxWaitSecs = 10`): conversa não atribuída em 10s dispara `sla.breached`, marca `slaBreachedAt` e registra auditoria. Evidência no Changelog. **Feito via B2-5** (`ROADMAP_BACKEND.md`, 2026-07-24) — `apps/api/test/sla.e2e-spec.ts`. | seed/fila de teste | ✅ 2026-07-24 |

---

## 🟠 Fase 4 — Testes no backend

**Problema:** o backend (auth multi-tenant, webhooks, filas — as partes de maior risco) tem **zero testes**; o frontend mock tem 66. A pirâmide está invertida.
**Meta:** módulos críticos cobertos por testes que rodam com `npm test` em `apps/api`.

| ID | Item | Referências | Status |
|---|---|---|---|
| F4-1 | Primeiro spec rodando (smoke de bootstrap do Jest + ts-jest já configurados). **Aceite:** `cd apps/api && npm test` executa e passa. **Feito via B4-1** (`ROADMAP_BACKEND.md`, 2026-07-22). | `apps/api/jest.config.js` · `apps/api/test/` (vazio hoje) | ✅ 2026-07-22 |
| F4-2 | Auth: `auth.service` (login, refresh, logout, senha errada, usuário inativo), `roles.guard`, `token-blacklist.service`. **Feito via B4-2** (`ROADMAP_BACKEND.md`, 2026-07-22). | `apps/api/src/modules/auth/` | ✅ 2026-07-22 |
| F4-3 | Isolamento multi-tenant: testes de regressão garantindo que conversation/contact/user **sempre** filtram por `companyId` (vazamento entre tenants é o pior bug possível num SaaS). **Feito via B4-3** (`ROADMAP_BACKEND.md`, 2026-07-22, cobrindo também os módulos novos de B1: tag/queue/notification/audit-log). | `apps/api/src/modules/conversation/conversation.service.ts` · `contact.service.ts` · `user.service.ts` | ✅ 2026-07-22 |
| F4-4 | Webhook: sem `EVOLUTION_API_KEY` → 500 fail-closed; apikey inválida → 403; normalização `messages.upsert` → `MESSAGES_UPSERT`; job enfileirado com payload correto. **Feito via B4-4** (`ROADMAP_BACKEND.md`, 2026-07-24). | `apps/api/src/modules/webhook/webhook.controller.ts` | ✅ 2026-07-24 |
| F4-5 | Envio de mensagem: `send-message.service` + `message-send.processor` (retry/backoff, transições de `MessageStatus`, falha da Evolution API). **Feito via B4-5** (`ROADMAP_BACKEND.md`, 2026-07-24) — nota: `message-send.processor` foi removido por decisão B2-1 (envio ficou síncrono), então o teste cobre `send-message.service` sem fila por trás. | `apps/api/src/modules/message/` | ✅ 2026-07-24 |
| F4-6 | Meta de cobertura ≥ 70% nos módulos auth, webhook e conversation (`npm run test:cov`). Ajustar meta em Decisões se necessário. **Feito via B4-7** (`ROADMAP_BACKEND.md`, 2026-07-24) — meta cumprida em todos os módulos-alvo (auth, webhook, conversation, sla + os novos de B1). | `apps/api/jest.config.js` | ✅ 2026-07-24 |

---

## 🟡 Fase 5 — Higiene do repositório e documentação

**Problema:** ~20 relatórios de status na raiz (alguns afirmando mais do que o código entrega — ex.: SLA "pronto para deploy"); README descreve stack que não existe (`Next.js em apps/web`).
**Meta:** raiz limpa, documentação que reflete a realidade.

| ID | Item | Referências | Status |
|---|---|---|---|
| F5-1 | Criar `docs/archive/` e mover os relatórios da raiz (`ALL_FIXES_COMPLETE.txt`, `CORRECTIONS_*`, `SECURITY_*`, `FRONTEND_*`, `PROJECT_*`, `GIT_*`, `SLA_FIXES.md`, `START_HERE.txt`, `TESTE_*.md`, `CODE_QUALITY_CHECKLIST.md`, `COMO_TESTAR_AGORA.md`, `DELIVERABLES.md`); `LOGO_GUIDELINES.md` → `docs/`. Na raiz ficam: `README.md` + este roadmap. **Feito:** 20 relatórios movidos com `git mv` (histórico preservado) + `docs/archive/README.md` avisando que não são fonte de verdade. | raiz do repo | ✅ 2026-07-16 |
| F5-2 | Reescrever o `README.md` com a stack real (frontend Vite + React 19 JS na raiz; sem `apps/web`), passos de execução **validados** e roteiro de fases atualizado. **Feito:** stack corrigida, estrutura real, passos com os pitfalls conhecidos (`.env` da raiz com `EVOLUTION_API_KEY` — F8-9; subir serviços nominalmente por causa do `minio_init` quebrado — B-5; senha do seed é ALEATÓRIA em `.seed-credentials-*.txt` — F0-6), tabela de estado atual e ponteiros para o roadmap e o `API_CONTRACT.md`. | `README.md` | ✅ 2026-07-16 |
| F5-3 | Decisão de arquitetura do frontend: permanecer com Vite + React (recomendado para estabilizar agora) ou migrar para Next.js/TS como o README prometia. Registrar em Decisões e alinhar o README. **Decidido: permanece Vite + React** (registrado em Decisões; README e CLAUDE.md alinhados). | — | ✅ 2026-07-16 |
| F5-4 | Renomear pacote raiz `multi-atendimento` → `@atendehub/web` e atualizar descrição (deixou de ser "protótipo"?). **Feito:** nome e descrição atualizados; testes e build inalterados. | `package.json` | ✅ 2026-07-16 |
| F5-5 | Criar `CLAUDE.md` na raiz: comandos (dev/test/build front e back, docker, prisma), arquitetura resumida, convenções e apontador para este roadmap. **Feito:** inclui também os 5 pitfalls conhecidos (chave Evolution na raiz, minio_init, mídia criptografada, base64 p/ Evolution, janelas próprias no Windows) e as convenções (ícones svgrepo, commits PT, regras do doc vivo). | `CLAUDE.md` (novo) | ✅ 2026-07-16 |

---

## 🟡 Fase 6 — Hardening de segurança (pré-produção)

**Contexto:** a base de segurança do backend é boa (helmet, throttler global, ValidationPipe com whitelist, webhook fail-closed com comparação timing-safe, validação de secrets no boot, token blacklist). Os itens abaixo fecham as lacunas restantes.

| ID | Item | Referências | Status |
|---|---|---|---|
| F6-1 | Estratégia de tokens: access/refresh em `localStorage` é vulnerável a XSS. Migrar para cookie `httpOnly` + proteção CSRF, **ou** registrar formalmente o trade-off aceito em Decisões (com mitigações: CSP, sanitização). **Decidido via B5-1** (`ROADMAP_BACKEND.md`, 2026-07-24): mantém `localStorage` — migrar exigiria reescrever o cliente HTTP/WS do front no meio da pausa; trade-off registrado formalmente. Parte prática do lado backend (limpeza de refresh tokens acumulados) feita junto. | `src/services/api.js:32-45` | ✅ 2026-07-24 |
| F6-2 | Rotação de segredos de produção: garantir que nenhum default do compose (postgres `atendehub_secret`, redis `redis_secret`, minio, `evolution_api_key_dev`) chegue a produção; checklist no `.env.example` + revisão do `docker-compose.prod.yml`. **Feito via B5-2** (`ROADMAP_BACKEND.md`, 2026-07-24) — redis/minio/evolution agora exigem a variável (`${VAR:?...}`), sem fallback pro valor de dev. | `docker-compose.yml` · `docker-compose.prod.yml` · `apps/api/.env.example` | ✅ 2026-07-24 |
| F6-3 | Health readiness: expandir F0-1 com checagem de dependências (PostgreSQL, Redis) num endpoint `/health/ready` separado do liveness leve — para orquestração/nginx. **Feito via B5-3** (`ROADMAP_BACKEND.md`, 2026-07-24) — validado com Postgres/Redis reais. | backend (HealthModule) | ✅ 2026-07-24 |
| F6-4 | Higiene de logs: remover `console.log` que expõe conteúdo de mensagens no navegador e revisar logs do backend para não vazar payloads/PII em produção. **Lado backend feito via B5-4** (`ROADMAP_BACKEND.md`, 2026-07-24) — nível `debug` (vazava telefone) desligado em produção, login parou de logar e-mail. **Lado frontend: já estava resolvido, sem nunca ter sido fechado no roadmap.** Os `console.log` de `useConversations.js:48,64` citados na referência original não existem mais nessa forma — o `git log -S` mostra que o log que imprimia a mensagem inteira (`console.log('📬 Nova mensagem recebida:', message)`, incluindo `.text`/`.content`) foi trocado para logar só o `conversationId` no commit `ba110ee`, bem antes desta sessão, como efeito colateral de um refactor não relacionado a este item. Varredura em 2026-07-24 confirma: zero ocorrências de `console.*` logando conteúdo de mensagem/draft em todo `src/` (`components/`, `hooks/`, `services/`, `context/`) — todos os `console.*` restantes logam só id/status/mensagem de erro genérica. | `src/hooks/useConversations.js` · `src/services/*.js` · backend (winston) | ✅ 2026-07-24 |
| F6-5 | Revisão final de CORS (`CORS_ORIGINS` de produção), rate limits (`THROTTLE_*`) e headers (helmet) com os domínios reais. **Feito via B5-5** (`ROADMAP_BACKEND.md`, 2026-07-24) — boot falha (fail-hard) em produção/staging se `CORS_ORIGINS` estiver ausente ou for `*`. | `apps/api/src/main.ts:31-36` · `apps/api/src/app.module.ts:23-32` | ✅ 2026-07-24 |

---

## 🟢 Fase 7 — Pronto para produção

| ID | Item | Referências | Status |
|---|---|---|---|
| F7-1 | CI (GitHub Actions): lint + `tsc --noEmit` + testes front e back em cada push/PR. **Aceite:** pipeline verde no repositório `Cmdev2019/atendehub`. **Lado backend feito via B6-1** (`ROADMAP_BACKEND.md`, 2026-07-24) — `.github/workflows/api-ci.yml`, também resolveu o BL-1 (eslint.config.js nunca existiu). **Lado frontend concluído:** novo `.github/workflows/web-ci.yml` — `npm ci` → `npm test` → `npm run build`, disparado em push/PR que tocam `src/**`/`public/**`/`index.html`/`vite.config.js`/configs de teste/`package.json` (raiz, não `apps/**` — evita rodar em mudanças só de backend). Sem passo de lint: o front nunca teve `eslint` configurado (diferente do backend, que tinha a dependência instalada só sem config — aqui não há nem a dependência); adicionar lint ao front é escopo novo, não parte deste item. Sem `tsc --noEmit`: o front é JavaScript puro, sem TypeScript. | `.github/workflows/web-ci.yml` (novo) | ✅ 2026-07-24 |
| F7-2 | Builds de produção validados: `vite build` + `nest build` sem warnings críticos; revisão do `docker-compose.prod.yml` + nginx servindo front e proxy da API/WS. **Feito via B6-2** (`ROADMAP_BACKEND.md`, 2026-07-24) — `apps/api/Dockerfile` (novo) e `infra/nginx/Dockerfile` (novo, builda o `vite build` real e serve estático); eliminado o container `web` obsoleto (residual do plano Next.js pré-F5-3). Validado com builds reais no Docker. | `docker-compose.prod.yml` · `infra/nginx/nginx.conf` | ✅ 2026-07-24 |
| F7-3 | Observabilidade mínima: winston estruturado (JSON) com request-id, métricas básicas das filas Bull (jobs falhos/atrasados). **Feito via B6-3** (`ROADMAP_BACKEND.md`, 2026-07-24) — validado com tráfego real de webhook. | `apps/api/src/main.ts` · nest-winston | ✅ 2026-07-24 |
| F7-4 | Runbook: passos de deploy, rollback, reset de filas e troubleshooting comum, em `docs/RUNBOOK.md`. **Feito via B6-4** (`ROADMAP_BACKEND.md`, 2026-07-24). | `docs/` | ✅ 2026-07-24 |
| F7-5 | **[Descoberto em 2026-07-15]** O Redis Adapter do Socket.IO nunca é aplicado: em gateway com namespace (`/ws`), o `server` recebido em `afterInit` é o **Namespace**, não o `Server` — `server.adapter(...)` falha com `server.adapter is not a function` (visível no log de boot). Hoje o Socket.IO funciona apenas single-instance; o `TESTE_REDIS_ADAPTER.md` está incorreto. Corrigir aplicando o adapter no nível do `IoAdapter` (custom adapter em `main.ts`). **Feito via B2-6** (`ROADMAP_BACKEND.md`, 2026-07-22). | `apps/api/src/modules/events/events.gateway.ts:58-89` · `apps/api/src/main.ts` | ✅ 2026-07-22 |

---

## 🟠 Fase 8 — Administração & Configurações

**Origem:** solicitação do usuário em 2026-07-15 — reorganizar o menu vertical e concentrar a administração dentro de Configurações.
**Escopo:** área de Configurações (⚙️) com: conexão WhatsApp via QR Code, tema claro/escuro, criação e gestão de usuários com níveis de acesso (usuário comum = AGENT, líder de setor = SUPERVISOR, administrador = ADMIN) e grupos/setores (departamentos do backend).

| ID | Item | Referências | Status |
|---|---|---|---|
| F8-1 | Remover do menu vertical o canal WhatsApp e o botão "🔗 Conectar" (era um botão morto) — a conexão agora vive em Configurações → Conexões WhatsApp. | `src/components/Sidebar.jsx` | ✅ 2026-07-15 |
| F8-2 | Mover o toggle claro/escuro da Topbar para Configurações → Aparência. | `src/components/Topbar.jsx` · `src/components/settings/SettingsPanel.jsx` | ✅ 2026-07-15 |
| F8-3 | Criar a área de Configurações: navegação por seções, troca de view no Dashboard (Sidebar/⚙️ da Topbar), título dinâmico e visibilidade de seções por nível de acesso (Aparência: todos · Usuários: SUPERVISOR+ · WhatsApp/Grupos: ADMIN). | `src/components/settings/SettingsPanel.jsx` (novo) · `src/main.js` · `src/styles.css` | ✅ 2026-07-15 |
| F8-4 | Conexões WhatsApp via QR Code: criar conexão (POST `/whatsapp`), gerar/renovar QR (GET `/whatsapp/:id/qrcode`), status ao vivo (evento `connection.status` + polling de `/status` a cada 4s enquanto o QR está na tela), desconectar e excluir. **VALIDADO com celular real em 2026-07-15:** o usuário criou a conexão "carlos teste" pela tela, escaneou o QR e o status ficou `CONNECTED` (confirmado também via sync com a Evolution). Obs.: o WhatsApp exibiu "não é possível conectar novos dispositivos" em tentativas iniciais (rate-limit temporário por múltiplas tentativas) — resolveu sozinho ao insistir; versão Baileys conferida como atual (2.3000.1035194821). | `src/components/settings/SettingsPanel.jsx` · `apps/api/src/modules/whatsapp/` | ✅ 2026-07-15 |
| F8-5 | Usuários e níveis de acesso: criação (nome, e-mail, senha com regras do DTO, nível), mudança de nível inline, ativar/desativar, excluir. Rótulos do produto: Usuário comum (AGENT) · Líder de setor (SUPERVISOR) · Administrador (ADMIN). | `src/components/settings/SettingsPanel.jsx` · `apps/api/src/modules/user/` | ✅ 2026-07-15 |
| F8-6 | Grupos (setores): criar com nome+cor, listar com contagem de membros, excluir, expandir para gerenciar membros (adicionar/remover via `/departments/:id/users`). | `src/components/settings/SettingsPanel.jsx` · `apps/api/src/modules/department/` | ✅ 2026-07-15 |
| F8-7 | Configurações de perfil do usuário: troca da própria senha (`PATCH /users/:id/password`), avatar e edição de dados pessoais. **Concluído:** nova seção "Meu perfil" em Configurações, visível a qualquer usuário autenticado (não só admin) — edição de nome/telefone (`PATCH /users/me`, B3-1), upload de avatar (`POST /users/me/avatar`, multipart restrito a imagem, B3-2) e troca de senha (`PATCH /users/:id/password`, já existia no back, sem UI até agora). `AuthContext` ganhou `updateUser()` pra refletir a mudança no Topbar imediatamente, sem precisar de novo login. 3 ícones novos em `icons.jsx` (camera, lock, check — mesma coleção Tabler do svgrepo). **Achado e corrigido no caminho:** o mock de `refreshToken()` gerava um token sem o id do usuário embutido (`token_<ts>` em vez de `token_<id>_<ts>`), quebrando `getCurrentUser()` com "Usuário não encontrado" depois do 1º refresh — só apareceu porque o novo fluxo de "/me" depende de `getCurrentUser()` pra resolver a identidade; corrigido + teste de regressão novo. **Validado com Playwright real no navegador** (não só Jest): login mock → editar nome/telefone → "Perfil atualizado" + Topbar reativo → upload de avatar real (PNG) → avatar troca de iniciais pra imagem → trocar senha → "Senha atualizada com sucesso", sem erro de console além do ruído pré-existente do WebSocket em modo mock. | `src/components/settings/SettingsPanel.jsx` · `src/services/api.js` · `src/services/apiMock.js` · `src/context/AuthContext.jsx` · `src/components/icons.jsx` | ✅ 2026-07-24 |
| F8-8 | Configurações avançadas de grupos: filas por setor (estratégia de distribuição), vínculo conexão WhatsApp ↔ setor (`departmentId` no `CreateConnectionDto`). **Concluído:** nova seção "Filas de distribuição" em Configurações (`QueuesSection`, ADMIN+) — CRUD completo (`GET/POST/PATCH/DELETE /queues`) com nome, estratégia (round-robin/manual/menos ocupado), tempo máx. de espera, mensagem de saudação e setor vinculado (select opcional); ativar/desativar sem excluir. `WhatsappSection` ganhou select de setor no formulário de criação de conexão (`departmentId` no `POST /whatsapp`), com o setor vinculado exibido em cada conexão da lista. **Validado com Playwright real no navegador:** criar fila com setor → aparece na lista com os dados certos; editar (nome + demais campos) → atualiza in-place; desativar → badge muda pra "Inativa" e item fica esmaecido; criar conexão WhatsApp com setor → setor aparece vinculado na listagem. Com isso a Fase 8 fica 12/12 — só restam os itens puramente frontend deixados em aberto nas Fases 3/6/7 (fora do escopo desta fase). | `src/components/settings/SettingsPanel.jsx` · `src/services/api.js` · `src/services/apiMock.js` | ✅ 2026-07-24 |
| F8-9 | **[Descoberto ao gerar QR]** Chave da Evolution desalinhada: `apps/api/.env` tinha chave forte de 128 chars, mas o container subiu com o default `evolution_api_key_dev` (o compose lê `${EVOLUTION_API_KEY}` do `.env` da RAIZ, que não existia) → 401 em toda chamada API→Evolution. Corrigido: criado `.env` na raiz (gitignored) com a mesma chave e container recriado. | `.env` (raiz, novo) · `docker-compose.yml` | ✅ 2026-07-15 |
| F8-11 | **[Pedido do usuário 2026-07-16]** Remodelar os ícones do produto: substituir TODOS os emojis da UI por ícones SVG do svgrepo.com. Criado `src/components/icons.jsx` (componente `Icon` + registro de 34 SVGs inline da coleção "Tabler Icons", disponível no svgrepo, MIT) e substituídos os emojis em 8 componentes (Sidebar, Topbar, ChatPanel, ConversationQueue, CustomerPanel, SettingsPanel, LoginForm, DemoBanner). **Convenção:** ícone novo = svgrepo.com, preferindo a mesma coleção. Obs.: svgrepo.com respondia 429 a clientes não-navegador no dia; os mesmos SVGs foram baixados da fonte canônica da coleção (GitHub tabler-icons). | `src/components/icons.jsx` (novo) · `src/components/*.jsx` · `src/styles.css` | ✅ 2026-07-16 |
| F8-10 | **[Descoberto na sequência]** Webhooks reais seriam rejeitados com 403 após o pareamento: a Evolution gera um token aleatório por instância e o envia no campo `apikey` dos webhooks, mas o `WebhookController` valida contra a chave global. Corrigido: `createInstance` agora passa `token` = chave global (instância `e2e-session` recriada assim). | `apps/api/src/modules/whatsapp/evolution.service.ts:76-86` · `apps/api/src/modules/webhook/webhook.controller.ts` | ✅ 2026-07-15 |
| F8-12 | **[Reportado pelo usuário 2026-07-17]** Conexão pareada exibia "Conectado" + "Sem número pareado" (contraditório): o backend nunca capturava o número do dono — parse no shape da Evolution **v1** enquanto a nossa é v2.3.4. 3 defeitos: (a) `fetchInstance` procurava `i.instance?.instanceName` (v2 retorna array plano com `name`) → sempre `null`, silenciosamente; (b) campos v1 (`phoneNumber`/`profilePictureUrl`) em vez de `ownerJid` (com sufixo `@s.whatsapp.net` e às vezes `:device`) e `profilePicUrl`; (c) webhook CONNECTION_UPDATE lia `data.phoneNumber ?? data.me.id`, mas o v2 manda `wuid`. Corrigido: `fetchInstance` normaliza o shape v2 (compat. v1 mantida), helper `jidToPhone`, webhook extrai `wuid`/`profilePictureUrl` e repassa a foto ao service. | `apps/api/src/modules/whatsapp/evolution.service.ts` · `apps/api/src/modules/webhook/webhook.service.ts` | ✅ 2026-07-17 |

---

## 📋 Backlog

Itens identificados mas ainda não priorizados em fase. Ao priorizar, mover para a fase adequada com ID definitivo.

| ID | Item | Referências | Status |
|---|---|---|---|
| B-1 | Upload real de anexos: a UI do chat aceita arquivos/imagens (commit `6c96242`), mas não há integração com a API/MinIO (`StorageService` existe no back). Ligar front → upload → `Attachment`. **Implementado em F1-11** (prints colados + arquivos do seletor, qualquer tipo até 16MB, com preview e caption). | `src/components/ChatPanel.jsx` · `apps/api/src/shared/storage/storage.service.ts` | ✅ 2026-07-16 |
| B-2 | Dashboard/Métricas com dados reais (hoje calculadas sobre mock). Provável endpoint agregado no backend. **Concluído (2026-07-24):** a descrição já estava um pouco desatualizada — desde F1-2 (2026-07-21) o `Metrics.jsx` já calculava sobre a lista de conversas *carregada* (real, não mock), só que limitada à página atual (sem endpoint agregado, quebraria assim que B-4 paginasse de verdade). Resolvido com o novo `GET /conversations/stats` (contagens via `count`/`aggregate` do Prisma, direto no banco, independentes de paginação): `totalActive`, `waiting`, `open`, `resolvedToday` (novo indicador, não existia antes — usa o campo `resolvedAt` que já estava no schema sem uso), `unreadCount`, `unreadConversations`. `useConversations.js` ganhou `fetchStats()` (chamado no mount + reaproveitado dentro dos handlers de socket já existentes — `conversation.created`/`message.new`/`conversation.updated`/`conversation.assigned` — para manter os números ao vivo sem registrar NENHUM listener novo). **Achado no caminho:** `wsClient.off(event)` remove **todos** os listeners daquele nome de evento, não só o do chamador — registrar um listener próprio no `Metrics.jsx` correria o risco de apagar os do `useConversations.js` ao trocar pra tela de Configurações (ambos desmontam/remontam de formas diferentes). Contornado reaproveitando os handlers existentes em vez de arriscar a fragilidade do pub-sub (fica registrado como fragilidade conhecida, não corrigida — não fazia parte do escopo de B-2). **Validado de ponta a ponta:** `GET /conversations/stats` real (login real, token novo) retorna o shape esperado sem colidir com `/:id`; visualmente com Playwright, os 5 cards renderizam com dado real do mock. | `apps/api/src/modules/conversation/conversation.service.ts` · `conversation.controller.ts` · `src/hooks/useConversations.js` · `src/components/Metrics.jsx` · `src/services/api.js`/`apiMock.js` | ✅ 2026-07-24 |
| B-3 | Notificações e consulta de auditoria: os modelos `Notification` e `AuditLog` existem no schema, mas não há módulos/endpoints expostos (fase 10 do roteiro original). **Concluído via B1-3/B1-4** (`ROADMAP_BACKEND.md`, 2026-07-22) — módulos `notification/` e `audit-log/` completos, com `GET /notifications`, `PATCH /notifications/:id/read`, `GET /audit-logs` (ADMIN+). Achado só agora (2026-07-24) ao revisar o backlog — nunca tinha sido fechado neste documento apesar de resolvido há sessões. | `apps/api/src/modules/notification/` · `apps/api/src/modules/audit-log/` | ✅ 2026-07-22 |
| B-4 | Paginação/scroll infinito real na fila de conversas e no histórico de mensagens (o back já pagina; o front ignora). **Concluído (2026-07-24):** ambos os back-ends de paginação já existiam prontos (`GET /conversations` com `page`/`limit`/`totalPages`; `GET /conversations/:id/messages` com cursor `before` por id de mensagem) — o trabalho foi 100% frontend. **Fila de conversas:** `useConversations.js` ganhou `loadMoreConversations()` (busca a próxima página, acrescenta ao final com dedupe por id — cobre o caso de uma conversa nova ter chegado via socket entre duas páginas); `ConversationQueue.jsx` detecta scroll a 150px do fim da lista (`.queue-list`) e dispara sozinho, exceto com busca ativa (resultado filtrado não é sinal de fila completa). **Histórico de mensagens:** `useConversations.js` ganhou `loadMoreMessages(id)` (cursor = id da mensagem mais antiga já carregada, prepend no início do array); `ChatPanel.jsx` detecta scroll a 100px do topo do histórico e dispara — com cuidado extra pra não quebrar o auto-scroll-pro-fim existente (mensagem nova ainda desce a tela; mensagem antiga carregada preserva a posição visual, recalculando `scrollTop` pela diferença de `scrollHeight` antes/depois do prepend). | `src/hooks/useConversations.js` · `src/components/ConversationQueue.jsx` · `src/components/ChatPanel.jsx` | ✅ 2026-07-24 |
| B-5 | **[Descoberto em 2026-07-15]** `docker compose up` falha ao baixar `minio/mc:RELEASE.2025-06-13T11-33-47Z` (tag não existe mais no Docker Hub), o que aborta também o pull do MinIO. Postgres, Redis e Evolution sobem normalmente. Atualizar as tags (ou remover o container `createbuckets` e criar o bucket via `StorageService`, que já faz `makeBucket` no boot). **Concluído (2026-07-24):** removido o serviço `minio_init` por completo de `docker-compose.yml` e `docker-compose.prod.yml` — era redundante mesmo antes de quebrar (`StorageService#onModuleInit` já faz `makeBucket` + `setBucketPolicy` de leitura pública no boot da API, confirmado lendo o código). **Validado com volume do MinIO recriado do zero** (sem nenhum estado anterior mascarando o teste): log da API mostra `Bucket "atendehub-media" criado.` na primeira subida, sem o container auxiliar. Bônus: `docker compose up -d` **sem listar serviços** (o comando que antes falhava, motivo do pitfall #2 no `CLAUDE.md`) agora funciona normalmente — pitfall removido de `CLAUDE.md`/`README.md`/`docs/RUNBOOK.md`. | `docker-compose.yml` · `docker-compose.prod.yml` · `CLAUDE.md` · `README.md` · `docs/RUNBOOK.md` | ✅ 2026-07-24 |
| B-7 | **[Descoberto em 2026-07-24, durante B-2]** `wsClient.off(event)` (`src/services/websocket.js`) remove **todos** os listeners registrados pro nome do evento, não só o do componente que chama — dois componentes ouvindo o mesmo evento (ex.: `useConversations.js` e um futuro segundo consumidor de `conversation.updated`) correm risco de um apagar o listener do outro ao desmontar. Contornado em B-2 (métricas reaproveitam handlers existentes em vez de registrar novos), mas a fragilidade do pub-sub em si não foi corrigida. Fix correto: `on()` devolver uma função de unsubscribe (ou `off()` aceitar o callback específico a remover), como a maioria das implementações de event emitter. **Concluído 2026-07-25:** lógica de pub/sub extraída para `src/services/eventBus.js` (`EventBus`, testável sem depender de `import.meta.env`, que impedia importar `websocket.js` direto em teste — mesma razão pela qual `api.test.js` já mockava `./api`); `off(event, callback)` agora remove só o callback informado (filtra por referência), `on()` passou a devolver uma função de unsubscribe, e `off(event)` sem callback continua removendo todos por compatibilidade. Todos os 8 pontos que chamavam `wsClient.on/off` no app (`useConversations.js` ×7, `SettingsPanel.jsx` ×1) foram migrados para handlers nomeados + `off(event, handler)` explícito, em vez de depender do valor de retorno de `on()` (mantém os mocks de teste existentes — `jest.fn()` sem `mockImplementation` — funcionando sem reescrever toda a suíte). | `src/services/websocket.js` · `src/services/eventBus.js` (novo) · `src/hooks/useConversations.js` · `src/components/settings/SettingsPanel.jsx` | ✅ 2026-07-25 |
| B-6 | **[Descoberto em 2026-07-15]** A fila `message-send` tem processor registrado mas **nenhum produtor** — o envio real é 100% síncrono no `send-message.service` (Evolution → persiste → emite). Decidir o caminho único (síncrono vs fila com retry/backoff) e remover o caminho morto. **Concluído via decisão B2-1** (`ROADMAP_BACKEND.md`, 2026-07-22) — fila removida por completo (`message-send.processor.ts`, registro no módulo e `QUEUE_NAMES.MESSAGE_SEND` deletados); envio é 100% síncrono, único caminho. Achado só agora (2026-07-24) ao revisar o backlog — nunca tinha sido fechado neste documento. | `apps/api/src/modules/message/send-message.service.ts` | ✅ 2026-07-22 |
| B-8 | **[Descoberto em auditoria 2026-07-25]** O backend expõe `GET /notifications`, `PATCH /notifications/:id/read` e `PATCH /notifications/read-all` desde B1-3 (`ROADMAP_BACKEND.md`, 2026-07-22) — inclusive já dispara notificação real em SLA estourado (fan-out pra todo `SUPERVISOR+`) e em conversa atribuída. Nenhum componente do frontend consome esse módulo: busca por "notification" em `src/` não retorna nenhum arquivo. Hoje um supervisor só sabe de um SLA violado olhando a fila (badge de F3-4) — quem não está com a tela aberta simplesmente não é avisado, apesar do backend já ter sido construído pra isso. **Depende de corrigir B-7 antes** (seria o 2º consumidor real do mesmo tipo de evento socket). **Concluído 2026-07-25** (depois de B-7): sino de notificações real no Topbar. `apiClient` ganhou `getNotifications`/`markNotificationRead`/`markAllNotificationsRead` (+ roteamento no mock); novo hook `useNotifications.js` busca no mount e reage a `sla.breached`/`conversation.assigned` (os 2 únicos eventos que o backend usa pra criar notificação, B1-3) — sem socket push dedicado, o backend só expõe REST; novo componente `NotificationBell.jsx` (badge de não lidas, dropdown com lista, marcar uma/todas como lidas, fecha ao clicar fora), plugado no `Topbar.jsx` no lugar do botão de sino que já existia mas não fazia nada. Mock ganhou 2 notificações seedadas pro admin (SLA estourado não lida + conversa atribuída já lida) pra a demo não nascer vazia. **Validado visualmente 2026-07-25** com Playwright real (Chromium instalado nesta sessão via `npx playwright install chromium` — o ambiente passou a ter navegador disponível): sino abre/fecha o dropdown, mostra "Nenhuma notificação por aqui." corretamente com fila real vazia, fecha ao clicar fora. Validação encontrou (e esta sessão corrigiu) 2 bugs reais não relacionados ao componente em si, registrados como **B-22**/**B-23**. | `src/components/NotificationBell.jsx` (novo) · `src/hooks/useNotifications.js` (novo) · `src/services/api.js` · `src/services/apiMock.js` · `src/components/Topbar.jsx` · `src/styles.css` | ✅ 2026-07-25 |
| B-9 | **[Descoberto em auditoria 2026-07-25]** Não existe nenhuma forma de dar onboarding a uma empresa (tenant) nova: `Role.SUPER_ADMIN` existe no enum e nos guards, mas não há endpoint para criar `Company` nem para criar um usuário `SUPER_ADMIN` — `user.service.ts` bloqueia explicitamente essa criação via API. A única forma de nascer uma empresa hoje é rodar `apps/api/prisma/seed.ts` manualmente. Para um produto que se descreve como "SaaS multi-tenant", isso trava qualquer segundo cliente real sem intervenção manual no banco. Decidir: endpoint de auto-cadastro de empresa, painel super-admin separado, ou processo manual documentado como decisão consciente (registrar em Decisões). **Decisão do usuário: auto-cadastro público** (ver Registro de decisões) — quem se cadastra vira `ADMIN` da empresa nova, sem `SUPER_ADMIN`/aprovação manual no meio. **Concluído 2026-07-25** (depois de B-20, pré-requisito): novo `POST /auth/register-company` (público, `@Throttle` 3/60s por IP — mais apertado que login, pois cria linhas no banco), `AuthService#registerCompany` — valida e-mail único globalmente, gera `slug` único a partir do nome da empresa (normalizado sem acento/pontuação, colisão resolvida com sufixo `-2`/`-3`/...), cria `Company` (plano `FREE` por padrão, sem billing) + `User` `ADMIN` numa `$transaction`, e devolve o mesmo shape do login (auto-login, sem 2ª ida à tela). Frontend: `RegisterCompanyForm.jsx` novo (mesmo visual do `LoginForm`), `LoginPage.jsx` ganhou alternância login↔cadastro num único card (sem rota nova — SPA de tela única), `AuthContext#registerCompany` espelhando `login`, validação client-side da política de senha (`validators.passwordPolicy`, mesma regra do backend) pra não gastar uma ida ao servidor com senha fraca. **Validado de ponta a ponta com API + Postgres reais** (Docker Desktop religado nesta sessão): cadastro real com nome acentuado ("Café & Confeitaria Açúcar!!") → 201 com tokens + slug gerado corretamente (`cafe-confeitaria-acucar`, bytes conferidos em hex no Postgres — não só a renderização do terminal); e-mail duplicado → 409; dados de teste removidos do banco depois. **Validado visualmente 2026-07-25** com Playwright real (Chromium instalado nesta sessão): formulário de cadastro renderiza e submete corretamente, auto-login funciona, dashboard mostra o nome do usuário recém-criado. **Achado crítico na validação:** a empresa nova (fila de conversas real e legitimamente vazia) aparecia mostrando a MESMA conversa fixture ("Marina Alves") que a conta admin via — não era coincidência nem vazamento entre tenants de verdade (confirmado via curl: `companyId` diferente, `GET /conversations` da empresa nova retorna `[]` de verdade), e sim um bug totalmente separado no frontend (**B-22**) que mantinha dados mock na tela por trás de um backend real e conectado. Corrigido e revalidado no mesmo navegador (screenshot final mostra "Nenhuma conversa" real, sem o fixture). | `apps/api/src/modules/auth/auth.service.ts` · `apps/api/src/modules/auth/auth.controller.ts` · `apps/api/src/modules/auth/dto/register-company.dto.ts` (novo) · `src/components/RegisterCompanyForm.jsx` (novo) · `src/pages/LoginPage.jsx` · `src/context/AuthContext.jsx` · `src/services/api.js` · `src/services/apiMock.js` · `src/utils/validators.js` · `docs/API_CONTRACT.md` | ✅ 2026-07-25 |
| B-10 | **[Descoberto em auditoria 2026-07-25]** `docs/RUNBOOK.md` documenta deploy, rollback, reset de fila e troubleshooting, mas não tem nenhuma seção de backup/restore do PostgreSQL (dado principal do produto) nem do MinIO (mídias — fotos, áudios, documentos trocados no WhatsApp). Para um runbook que se propõe "produção pronta" é uma omissão real, não estética — hoje não há procedimento documentado para recuperar o banco ou os anexos em caso de perda de volume/corrupção. **Concluído 2026-07-25:** nova seção "Backup e restore" no `RUNBOOK.md`. Postgres: `pg_dump -Fc` dos dois bancos que vivem na mesma instância (`atendehub`, prioridade, e `evolution`, secundário — perder sessão do WhatsApp só exige re-parear via QR) + `pg_restore` (banco novo ou `--clean --if-exists` por cima de um vivo, com aviso de testar num banco separado antes). MinIO: tar do volume Docker via container Alpine descartável (mais simples que `mc mirror` — o projeto já apanhou da imagem `minio/mc` em B-5) + ressalva honesta registrada no texto (não é backup S3-nativo; janela pequena de pegar arquivo em escrita se tirado com upload em andamento — aceitável pro porte do projeto). **Validado de verdade** contra o ambiente local: dump do Postgres restaurado num banco separado (`atendehub_restore_test`) com contagem de linhas idêntica (`users`: 1=1); tarball do MinIO extraído num volume novo, estrutura de bucket íntegra (`atendehub-media/`) — achado no caminho: comparar contagem de arquivos do volume original vs. restaurado direto não é um teste válido de integridade num MinIO **ao vivo** (o próprio serviço escreve metadata em segundo plano entre o backup e a comparação — diferença de contagem observada era esperada, não um defeito do backup); documentado como ressalva no lugar de uma comparação falsa de "restore perfeito". 2 bugs de ambiente (não do produto) resolvidos no caminho: caminho `-f` do `pg_dump` dentro do container mangling no Git Bash (contornado com redirecionamento `>` em vez de `-f`) e `MSYS_NO_PATHCONV=1` necessário pros volumes `/data`/`/backup` do container Alpine não virarem caminho Windows. | `docs/RUNBOOK.md` | ✅ 2026-07-25 |
| B-11 | **[Descoberto em auditoria 2026-07-25]** `docs/API_CONTRACT.md` afirma cobrir "todos" os recursos REST, mas `GET /conversations/:conversationId/messages/:id` existe em `message.controller.ts` e não está documentado — único furo encontrado numa auditoria por amostragem que, fora isso, achou o contrato fielmente alinhado ao código real. **Concluído 2026-07-25:** seção nova no contrato, escrita a partir da leitura direta de `message.service.ts#findOne` (não um chute) — documenta 2 diferenças reais em relação à listagem que ninguém tinha registrado antes: (1) a busca por id **não** filtra `isDeleted`, então retorna mensagens apagadas (soft delete) que a listagem esconde; (2) o `select` de `attachments` aqui não traz `width`/`height`/`duration`, que a listagem traz — assimetria existente no código, agora pelo menos documentada. Item puramente documental, sem mudança de código. | `docs/API_CONTRACT.md` | ✅ 2026-07-25 |
| B-12 | **[Descoberto em auditoria 2026-07-25]** Não existe nenhum React Error Boundary em todo o `src/` — `main.js` monta a árvore direto, sem `componentDidCatch`/boundary. Uma exceção não tratada durante o render de qualquer componente derruba a SPA inteira (tela branca), sem nenhum fallback amigável ou log específico do incidente. **Concluído 2026-07-25:** `ErrorBoundary.jsx` novo (classe — é a única forma do React de capturar erro de render, não tem hook equivalente), envolvendo `AppContent` dentro do `AuthProvider` em `main.js`; fallback com ícone, mensagem e botão "Recarregar página" (`onReload` injetável via prop, default `window.location.reload()` — decisão de design puramente pra viabilizar teste, já que o jsdom deste projeto não deixa mockar `window.location.reload` por ser read-only). `componentDidCatch` loga só a mensagem/stack do erro (nunca dado do usuário, mesma convenção de F6-4/B5-4). | `src/components/ErrorBoundary.jsx` (novo) · `src/main.js` · `src/styles.css` | ✅ 2026-07-25 |
| B-13 | **[Descoberto em auditoria 2026-07-25]** 7 pontos usam `alert()`/`confirm()` nativos do navegador em vez de um modal próprio: `Topbar.jsx:11`, `Sidebar.jsx:22` (logout) e `SettingsPanel.jsx:182,192,354,691,844` (desconectar WhatsApp, excluir conexão/usuário/fila/grupo — todas ações destrutivas). Destoa do resto da UI, que já tem um design system próprio trabalhado (modais, banners). **Concluído 2026-07-25:** `ConfirmContext.jsx`/`useConfirm.js` novos — API imperativa `await confirm(mensagem, opções?)` com a mesma ergonomia do `confirm()` nativo (retorna um booleano), só que resolvida por uma Promise e renderizada como `ConfirmDialog.jsx` (modal próprio, tema-aware, fecha por overlay/Escape, foca o botão "Cancelar" por padrão — ação seguindo o mesmo princípio do resto da UI de não assumir a ação destrutiva). `ConfirmProvider` registrado uma vez em `main.js` (entre `AuthProvider` e `ErrorBoundary`), disponível pra qualquer componente da árvore. Os 7 pontos migrados: 2 idênticos (logout em `Topbar.jsx`/`Sidebar.jsx`) + 5 dentro de `SettingsPanel.jsx` (WhatsApp: desconectar/excluir; Usuários/Filas/Grupos: excluir) — as 5 exclusões marcadas com `{ danger: true }` (botão de confirmar em vermelho). | `src/context/ConfirmContext.jsx` (novo) · `src/hooks/useConfirm.js` (novo) · `src/components/ConfirmDialog.jsx` (novo) · `src/main.js` · `src/components/Topbar.jsx` · `src/components/Sidebar.jsx` · `src/components/settings/SettingsPanel.jsx` · `src/styles.css` | ✅ 2026-07-25 |
| B-14 | **[Descoberto em auditoria 2026-07-25]** Acessibilidade desbalanceada: só 10 ocorrências de `aria-*`/`role=` em 7 arquivos contra 26 `onClick` em 6 arquivos de componentes — a maioria dos botões de ação (menus, ações em `SettingsPanel.jsx`/`ChatPanel.jsx`) provavelmente não tem rótulo acessível correspondente. Não é um item novo de escopo, é dívida técnica que nunca foi auditada explicitamente em nenhuma fase anterior. **Concluído 2026-07-25:** auditoria botão a botão nos 10 arquivos com `onClick` — a suspeita inicial (rótulo ausente) só se confirmou de fato em 0 casos: todo botão só-com-ícone já tinha nome acessível via `title` e/ou `label` da prop do `Icon` (que vira `aria-label`+`role="img"` no SVG, herdado pelo botão na computação de nome acessível). O que a auditoria achou de real: **(1)** 3 listas/menus com item "ativo" indicado só por classe CSS, invisível pra leitor de tela — fila de conversas (`ConversationQueue.jsx`), menu lateral (`Sidebar.jsx`) e nav de Configurações (`SettingsPanel.jsx`) ganharam `aria-current` no item selecionado, atualizado dinamicamente; **(2)** 3 botões de excluir (conexão WhatsApp, fila, grupo) tinham nome acessível só via o `aria-label` do ícone, sem `title` — sighted mouse users não tinham tooltip explicando o ícone; adicionado `title` nos 3. Achado à parte, fora do escopo deste item, registrado como **B-21**: 2 botões decorativos sem `onClick` (busca em `ConversationQueue.jsx`, "Mais opções" em `ChatPanel.jsx`) — têm nome acessível, mas não fazem nada ao ativar. | `src/components/Sidebar.jsx` · `src/components/ConversationQueue.jsx` · `src/components/settings/SettingsPanel.jsx` | ✅ 2026-07-25 |
| B-15 | **[Descoberto em auditoria 2026-07-25]** O endpoint de webhook (`POST /api/v1/webhooks/evolution`) não tem `@Throttle` dedicado — só herda o limite global do throttler (100 req/60s), diferente de `/auth/login` e `/auth/refresh`, que já têm limites específicos mais apertados. É uma rota pública (`@Public()`, validada só por `apikey`) exposta a abuso de um IP malicioso consumindo a cota global e afetando outras rotas. **Concluído 2026-07-25:** `@Throttle({ default: { limit: 200, ttl: 60_000 } })` na rota — mais generoso que o global (a Evolution manda um webhook por evento, um pico legítimo de tráfego real pode ser maior que o tráfego comum da API), mas finito e desacoplado do `THROTTLE_LIMIT` compartilhado com as demais rotas. | `apps/api/src/modules/webhook/webhook.controller.ts` | ✅ 2026-07-25 |
| B-16 | **[Descoberto em auditoria 2026-07-25]** `.github/workflows/api-ci.yml` e `web-ci.yml` existem e fazem sentido lidos, mas o próprio B6-1 (`ROADMAP_BACKEND.md`) já registrava que nunca rodaram de fato no GitHub Actions — só foram validados reproduzindo os comandos localmente (`act` não estava disponível). Sem confirmar a 1ª execução real (push/PR de verdade), não há garantia de que o workflow funciona no ambiente do Actions (cache, versão do runner, segredos do repositório). **Concluído 2026-07-25** (pedido do usuário, decisão: push direto pra `master` em vez de branch+PR — os 4 commits pendentes de B-15/21/B-25/B-26/B-27 já estavam prontos localmente e tocam tanto `apps/api/**` quanto `src/**`, disparando os dois workflows de uma vez pelo gatilho `push`). `git push origin master` (`46db08d..241cbf6`) confirmado publicamente via API do GitHub (repo é público, sem precisar de `gh auth login`): **API CI e Web CI rodaram de verdade no GitHub Actions pela 1ª vez, os dois `completed`/`success`** — cache do npm, runner Ubuntu, Node 20 e os 3 passos de cada workflow (lint+tsc+test no backend; test+build no front) funcionaram sem nenhum ajuste. | `.github/workflows/api-ci.yml` · `.github/workflows/web-ci.yml` | ✅ 2026-07-25 |
| B-17 | **[Descoberto em auditoria 2026-07-25]** Nenhuma política de retenção de dados, exportação/portabilidade ou anonimização documentada, apesar do produto processar PII de terceiros via WhatsApp (nome, telefone, mídia, conteúdo de conversa) — relevante para LGPD num produto brasileiro. `contact.deleted` já é auditado (B1-4), mas é hard delete simples, sem processo formal de atendimento a solicitação de titular de dados. **Concluído 2026-07-25** (pedido do usuário: "continue a partir do B-17"). Nova seção "Retenção de dados e LGPD" em `docs/RUNBOOK.md`: tabela do que é retido e por quanto tempo (indefinido em tudo — contato, conversa, mensagem, mídia, audit log, nenhum cron de expiração), processo hoje pra exclusão/portabilidade, e ressalvas honestas sobre o que ainda não está automatizado. **Pesquisando o mecanismo real de exclusão pra documentar corretamente, achado um bug real** registrado e corrigido como **B-28** (ver linha abaixo) — sem ele, a documentação estaria descrevendo um mecanismo que não funcionava. | `docs/RUNBOOK.md` | ✅ 2026-07-25 |
| B-18 | **[Descoberto em auditoria 2026-07-25]** Monitoramento de erros em produção (Sentry ou equivalente) chegou a ser citado em `docs/archive/CODE_QUALITY_CHECKLIST.md` como pendência, mas não foi herdado por nenhum item das Fases 6/7 nem de B5/B6 — só o winston estruturado (B6-3) foi entregue, que cobre logs, não captura/agrupamento de exceções com stack trace e alerta. Parece esquecimento no meio da reorganização em `ROADMAP_BACKEND.md`, não uma decisão consciente de escopo. **Concluído 2026-07-25** (decisão do usuário: Sentry SaaS, tier gratuito — apresentadas as alternativas, GlitchTip self-hosted ficou registrada como opção pra trocar depois, o SDK é o mesmo). Backend: `SentryExceptionFilter` (`shared/monitoring/sentry.ts`) estende o `BaseExceptionFilter` do próprio Nest — resposta HTTP ao cliente **não muda** (validado via `curl`: 404/400 idênticos a antes), só captura no Sentry as exceções inesperadas (5xx / não-`HttpException`), nunca erro de validação/404/409 (fluxo normal, não incidente). Frontend: `initMonitoring()` chamado no boot do `main.js`; `ErrorBoundary` (B-12) passou a reportar todo erro de render pro Sentry, não só pro console. **Sem `SENTRY_DSN`/`VITE_SENTRY_DSN` configurado, o SDK não inicializa — no-op seguro**, confirmado subindo a API real sem a variável (boot normal, respostas de erro idênticas). Mesma cautela de privacidade das sessões anteriores (B-17/B-28/B-29/B-30): `beforeSend` remove `request.data`/`cookies` do evento nos dois lados — nunca manda telefone/conteúdo de mensagem pro Sentry. `VITE_SENTRY_DSN` também wireado como build arg do `nginx` (só era usado por `VITE_API_URL`/`VITE_WS_URL` antes) pra funcionar em produção de verdade, não só em dev. | `apps/api/src/shared/monitoring/sentry.ts` (novo) · `apps/api/src/main.ts` · `src/services/monitoring.js` (novo) · `src/main.js` · `src/components/ErrorBoundary.jsx` · `infra/nginx/Dockerfile` · `docker-compose.prod.yml` · `docs/RUNBOOK.md` · `.env.example` (raiz e `apps/api`) | ✅ 2026-07-25 |
| B-20 | **[Descoberto em 2026-07-25, discutindo o desenho do B-9]** `email` do `User` só é único **por empresa** (`@@unique([companyId, email])` em `schema.prisma:149`) — mas o login (`auth.service.ts#validateUser`) busca por `prisma.user.findFirst({ where: { email, isActive: true } })` **sem filtrar por `companyId`**, porque a tela de login não pede empresa, só e-mail+senha. Hoje isso nunca deu problema porque a única forma de nascer uma empresa é rodar o seed manualmente (baixíssima chance de e-mail colidir entre empresas). **Isso se torna um bug real e provável assim que B-9 (onboarding de tenant / auto-cadastro público) existir**: duas empresas diferentes cadastrando o mesmo e-mail (comum — ex. o mesmo dono testando duas contas, ou um freelancer administrando duas empresas) faria o login resolver para qualquer uma das duas de forma não-determinística (`findFirst` sem `orderBy` em possível empate de e-mail). **Pré-requisito de B-9**, não item independente. **Concluído 2026-07-25:** `@@unique([companyId, email])` trocado por `@@unique([email])` (migration `20260725120609_user_email_globally_unique`, aplicada contra o Postgres real do `docker compose` — Docker Desktop foi religado nesta sessão especificamente pra isso). `prisma/seed.ts` ajustado (o `upsert` do admin usava a chave composta `companyId_email`, que deixou de existir). **Achado corrigido no mesmo pacote (não estava no escopo original do item, mas é a mesma causa raiz):** `user.service.ts#create` checava e-mail duplicado só dentro da empresa (`findFirst({ companyId, email })`) — com a constraint agora global, um e-mail já usado por OUTRA empresa passaria pela checagem e quebraria com um 500 cru na hora do `create()` (violação de unique constraint não tratada); trocado para `findUnique({ email })` global, com o mesmo `ConflictException` 409 de antes. | `apps/api/prisma/schema.prisma` · `apps/api/prisma/migrations/20260725120609_user_email_globally_unique/` (novo) · `apps/api/prisma/seed.ts` · `apps/api/src/modules/user/user.service.ts` | ✅ 2026-07-25 |
| B-21 | **[Descoberto em 2026-07-25, durante a auditoria de acessibilidade do B-14]** 2 botões existem só visualmente, sem `onClick`: o ícone de lupa em `ConversationQueue.jsx` (o campo `<input type="search">` logo abaixo já faz a busca de verdade — o botão é vestigial) e o de "Mais opções" (`dots`) no cabeçalho do chat em `ChatPanel.jsx` (nunca teve menu implementado). Os dois já têm nome acessível (título/aria-label via `Icon`), então não é falta de rótulo — é um botão focável que não faz nada ao ser ativado, o que confunde quem navega por teclado/leitor de tela esperando uma ação. **Concluído 2026-07-25 (decisão: remover, não implementar):** os dois botões removidos — a busca de verdade já está no `<input type="search">`, e "Mais opções" nunca teve menu nenhum atrás; implementar uma ação nova pra um botão que hoje não existe em nenhum fluxo real seria escopo novo, não parte deste item. | `src/components/ConversationQueue.jsx` · `src/components/ChatPanel.jsx` | ✅ 2026-07-25 |
| B-19 | **[Descoberto em auditoria 2026-07-25]** A decisão de manter tokens em `localStorage` (F6-1/B5-1) foi tomada quando o frontend estava pausado, com o argumento explícito de não travar o fechamento do backend. Agora que Dockerfiles, nginx e CI de produção já existem e um deploy real está mais próximo, vale reabrir essa decisão antes do primeiro cliente real — quanto mais o produto avança, mais caro fica migrar para cookie `httpOnly`+CSRF depois. Não é um bug, é um convite a revisitar uma decisão com prazo de validade. **Reaberta e decidida 2026-07-25** (pedido do usuário: "me fale sobre cada um deles... qual é o melhor pra nossa aplicação"). Análise concreta contra o código real (não genérica): (1) produção já roda `app.atendehub.com`/`api.atendehub.com` — subdomínios do mesmo site, então cookie `SameSite=Lax` funcionaria sem precisar do modo `None`; (2) CORS já está pronto pra cookie cross-origin (`credentials:true` + lista explícita de origens, nunca `*`) — zero retrabalho aí; (3) o ponto que dá trabalho de verdade é o WebSocket — `websocket.js:41`/`events.gateway.ts:65-68` leem o token do `localStorage` pra autenticar o handshake do Socket.IO, precisaria aprender a ler do cookie; (4) risco de XSS hoje é mais baixo que a média — único `dangerouslySetInnerHTML` do projeto é em `icons.jsx`, SVGs fixos do próprio código, nunca conteúdo de mensagem/input do usuário (conferido em todo `src/`). **Decisão do usuário: manter `localStorage` por agora** — risco real hoje é baixo, custo de migrar (WebSocket + CSRF + reteste completo do fluxo de auth) é real, sem cliente pagante ainda. Gatilho claro pra reabrir registrado no Registro de decisões. | `src/services/api.js` · Registro de decisões (F6-1, e nova entrada abaixo) | ✅ 2026-07-25 (decisão: manter) |
| B-22 | **[Descoberto na validação visual manual de B-8/B-9, 2026-07-25]** `useConversations.js` inicializava `conversations` com o fixture mock (`initialConversations`) e só o substituía por dados reais **se** `response.data.length > 0` — uma fila real e legitimamente vazia (empresa nova via B-9, ou banco sem conversas ainda) deixava o mock na tela pra sempre, sem qualquer banner. Bug totalmente separado do mecanismo oficial de mock (`apiClient.mockActive`/`DemoBanner`, F2-1/F2-2) — não passava por ele, então o aviso "🧪 Modo demonstração" nunca aparecia. Achado concretamente: uma empresa nova cadastrada via Playwright real mostrava a mesma conversa fixture "Marina Alves" que a conta admin, apesar de ter `companyId` genuinamente diferente (confirmado via curl comparando os dois tokens). **Concluído 2026-07-25:** qualquer resposta válida do backend (mesmo array vazio) agora substitui o estado inicial — `if (response.data)` no lugar de `if (response.data && response.data.length > 0)`. Teste de regressão novo garante que `conversations` vira `[]` (não o fixture) quando o backend responde vazio. | `src/hooks/useConversations.js` · `src/hooks/useConversations.integration.test.js` | ✅ 2026-07-25 |
| B-23 | **[Descoberto na mesma validação, ao clicar em "Sair" de verdade]** `apiClient.logout()` não enviava `refreshToken` no corpo de `POST /auth/logout`, mas o backend exige (`RefreshTokenDto` com `@IsNotEmpty`) — 400 real, reproduzido via curl com a chamada exata do frontend. `AuthContext#logout` engolia esse erro no `catch` sem chamar `setUser(null)`: o modal de confirmação (B-13) fechava normalmente, mas a tela nunca voltava ao login — mesmo com os tokens já limpos do `localStorage` (feito no `finally` de `apiClient.logout`), a sessão ficava "presa" autenticada na UI. **Concluído 2026-07-25:** `logout()` agora envia `{refreshToken}` no corpo; `AuthContext#logout` limpa `user`/`error` em `finally`, independente do resultado da chamada ao backend (revogação é best-effort — a sessão local sempre termina quando o usuário confirma o logout). `AuthContext.test.js` novo (não existia nenhum teste do contexto até agora) cobre exatamente esse caso: `apiClient.logout()` rejeitando não impede `setUser(null)`. | `src/services/api.js` · `src/context/AuthContext.jsx` · `src/context/AuthContext.test.js` (novo) | ✅ 2026-07-25 |
| B-24 | **[Descoberto corrigindo B-23]** Depois de passar a enviar `refreshToken`, o logout passou a receber `204 No Content` de verdade do backend — e `ApiClient#request` chama `response.json()` incondicionalmente, o que lança `SyntaxError: Unexpected end of JSON input` num corpo vazio (não é `ApiError` nem `TypeError`, então vazava cru pro console e pro chamador). Escopo restrito: `/auth/logout` e `/auth/revoke` são as únicas rotas do backend com `HttpStatus.NO_CONTENT` — as demais rotas `DELETE` (usuário, fila, grupo) usam `HttpStatus.OK` com corpo JSON de propósito, confirmado lendo os controllers, então não são afetadas. **Concluído 2026-07-25:** `request()` pula o parse quando `response.status === 204`. Revalidado no navegador real: logout completo (clique → confirmar → volta ao login) sem nenhum erro no console. | `src/services/api.js` | ✅ 2026-07-25 |
| B-25 | **[Descoberto em revisão de auditoria adicional, 2026-07-25 — "review complementar" pedida pelo usuário]** `npm audit --omit=dev` em `apps/api` aponta **20 vulnerabilidades nas dependências de produção** (1 crítica, 6 altas, 13 moderadas), nunca cobertas por nenhum item de B5/B6 — o hardening de segurança cobriu superfície de aplicação (CORS, secrets, blacklist, timing-safe compare) mas nunca supply-chain de dependências desatualizadas. Crítica: `tar` (via `@mapbox/node-pre-gyp`, hardlink path traversal). Altas: `multer` (5 CVEs de DoS — módulo usado no upload real de anexo/avatar), `lodash` (via `@nestjs/config`, code injection/prototype pollution), `fast-xml-parser`, `brace-expansion`. Moderadas: toda a família `@nestjs/core`/`platform-express`/`websockets`/`bull` desatualizada, `body-parser`, `qs`, `file-type` (via `@nestjs/common`). Frontend: `npm audit --omit=dev` limpo, 0 vulnerabilidades. Corrigir de verdade exige upgrade maior do NestJS 10→11 (o próprio `npm audit fix --force` confirma breaking change) — não é um `npm audit fix` sem custo; exige rodar a suíte inteira (176 testes) depois de subir a versão antes de considerar fechado. **Concluído 2026-07-25 (pedido do usuário: "faça o upgrade do nestjs para 11").** Upgrade completo `@nestjs/*` 10→11 (`core`/`common`/`platform-express`/`platform-socket.io`/`websockets`/`bull`/`jwt`/`passport` em `^11.x`; `config` em `^4.0.4`, que resolve o `lodash` vulnerável; `throttler`/`schedule`/`mapped-types` continuam compatíveis via peer range, atualizados à última patch). `bcrypt` 5→6: elimina `@mapbox/node-pre-gyp`/`tar` por completo (a v6 trocou o build nativo para `node-gyp-build` com prebuilds — não usa mais `tar` em nenhum lugar da árvore). `multer` 1→2 (só troca de `Express.Multer.File`/`FileInterceptor`, sem `diskStorage`/config direta no código — nenhuma mudança de uso necessária). `brace-expansion@5.0.7` (via `glob` do `@nestjs/cli`) tinha fix disponível só na mesma major — `npm overrides` **escopado por versão** (`"brace-expansion@^5.0.0": "^5.0.8"`) força só essa cadeia; a 1ª tentativa sem escopo forçou também a cadeia do ESLint (que usa `brace-expansion@1.x`, API incompatível) e quebrou `npm run lint:check` (`TypeError: expand is not a function`) — corrigido restringindo o override, revalidado com lint limpo. **Resultado:** `npm audit --omit=dev` cai de 20 para **3 vulnerabilidades**, todas moderadas e a mesma raiz (`uuid <11.1.1` via `bull`, que ainda depende de `uuid@^8.3.0` — "No fix available", pacote em modo manutenção; risco real baixo porque nenhum caller do projeto passa buffer externo pra função de uuid, o gatilho da CVE). Migrar `bull`→`bullmq` resolveria de vez, mas é escopo maior que "upgrade do Nest" — registrado como decisão em aberto, não um item novo ainda. **Bug de tipos real encontrado e corrigido no caminho:** `@types/jsonwebtoken` (transitivo, puxado pela nova versão do `@nestjs/jwt`) apertou `expiresIn` de `string` para o tipo literal `StringValue` da lib `ms` — `auth.service.ts#generateTokens` quebrava a build (`tsc`) mesmo passando exatamente o mesmo valor de sempre; corrigido com cast explícito pro tipo público `JwtSignOptions['expiresIn']` (não muda nada em runtime, só relaxa a checagem estática pra um formato que o projeto já validava com `parseExpiry`). | `apps/api/package.json` | ✅ 2026-07-25 |
| B-26 | **[Descoberto em revisão de auditoria adicional, 2026-07-25]** Não existe nenhum fluxo de recuperação de senha — nem self-service, nem administrativo. `auth.controller.ts` só expõe `login`/`register-company`/`refresh`/`logout`/`revoke`/`me`; `PATCH /users/:id/password` (`user.controller.ts`) exige a senha atual via `bcrypt.compare` (`user.service.ts#changePassword`) — é troca de senha própria, não reset, e não existe nenhum endpoint que permita a um `ADMIN` resetar a senha de outro usuário sem sabê-la. Não há infraestrutura de e-mail (SMTP/nodemailer) em nenhum lugar do projeto. Isso era tolerável enquanto só `apps/api/prisma/seed.ts` criava empresas manualmente; **desde B-9 (auto-cadastro público)**, qualquer pessoa pode virar `ADMIN` de uma empresa nova e, se esquecer a senha, fica **permanentemente trancada fora da própria empresa** — nem ela mesma nem ninguém no sistema tem como destrancar. Gap de produto acoplado diretamente à decisão de B-9, que deveria ter sido levantado junto. **Decisão do usuário 2026-07-25: opção (b), reset administrativo mínimo** — sem SMTP, paliativo mais barato (ver Registro de decisões). **Concluído 2026-07-25:** novo `POST /users/:id/reset-password` (`ADMIN`-only, corpo vazio) — `UserService#resetPassword` gera senha temporária de 12 caracteres já dentro da política (maiúscula/minúscula/número, sem caracteres ambíguos tipo `0`/`O`/`1`/`I`/`l`), hasheia e substitui a senha do usuário-alvo, registra `user.password_reset` em auditoria (B1-4) e devolve `{ temporaryPassword }` em texto puro **só nesta resposta** (nunca logado nem persistido fora do hash). Bloqueia auto-reset (`id === requesterId` → 400, direciona pra `PATCH /users/:id/password`) — reforça a limitação conhecida do escopo (b): não resolve o ADMIN único da empresa se trancar sozinho, só cobre `AGENT`/`SUPERVISOR` travado com um `ADMIN` ativo por perto. Frontend: botão "Resetar senha" por usuário em `SettingsPanel.jsx` (oculto na própria linha do admin logado), confirmação via `useConfirm` (B-13) antes de invalidar a senha atual, e banner dismissível exibindo a senha gerada pra o admin repassar por fora (verbal/chat) — sem nenhuma tela intermediária que a persista. `docs/API_CONTRACT.md` documentado no mesmo commit. 3 testes novos em `user.service.spec.ts` (bloqueio de auto-reset, isolamento multi-tenant, geração+hash+auditoria com regex de política real). | `apps/api/src/modules/user/user.service.ts` · `apps/api/src/modules/user/user.controller.ts` · `apps/api/src/modules/user/user.service.spec.ts` · `src/services/api.js` · `src/services/apiMock.js` · `src/components/settings/SettingsPanel.jsx` · `docs/API_CONTRACT.md` | ✅ 2026-07-25 |
| B-27 | **[Descoberto em revisão de auditoria adicional, 2026-07-25]** B1-1 (`ROADMAP_BACKEND.md`) entregou CRUD completo de `Tag` + atribuição em conversa/contato (`POST/DELETE /conversations/:id/tags/:tagId`, `apps/api/src/modules/tag/`), mas **nenhum ponto do frontend chama esses endpoints** — busca em `src/services/api.js` por `tag`/`Tag` não retorna nenhuma ocorrência. O mock e os componentes já sabem *renderizar* `conversation.tags` (existe desde F1-3), mas não existe nenhuma UI para criar uma tag ou atribuí-la/removê-la de uma conversa — mesma classe de problema que motivou B-8 (endpoint pronto, zero consumidor), só que ainda não corrigida. **Concluído 2026-07-25.** `apiClient` ganhou `getTags/createTag/updateTag/deleteTag/addConversationTag/removeConversationTag` (real + mock, com catálogo de 5 tags seedado no mock a partir das fixtures já existentes). Nova seção "Tags" em Configurações (visível a `SUPERVISOR+`, mesmo corte do backend): CRUD com nome+cor, contagem de uso por conversa/contato, exclusão via `useConfirm` (B-13). `CustomerPanel.jsx` ganhou o consumo de verdade: chips coloridos com botão de remover + seletor "Adicionar tag…" (só tags ainda não atribuídas), liberado a qualquer usuário autenticado (backend não restringe atribuição por role, só a tag em si). **Mudança de shape que acompanhou o item:** `toUiConversation` achatava `tags` pra array de strings (`tag.name`) — perdia o `id`, inviável pra atribuir/remover de verdade; agora `tags` chega como `{id,name,color}[]` na UI (teste do normalizador atualizado). | `src/services/api.js` · `src/services/apiMock.js` · `src/hooks/useConversations.js` · `src/components/CustomerPanel.jsx` (+ `CustomerPanel.test.js`, novo) · `src/components/settings/SettingsPanel.jsx` · `src/main.js` · `src/styles.css` | ✅ 2026-07-25 |
| B-28 | **[Descoberto pesquisando o mecanismo de exclusão de titular para B-17, 2026-07-25]** `ContactService#remove` fazia hard delete (`prisma.contact.delete`), mas `Conversation.contact` não tem `onDelete` configurado no schema (default do Postgres via Prisma = `RESTRICT`) — **excluir um contato com pelo menos uma conversa quebrava com violação de FK (erro P2003 não tratado, provavelmente um 500 cru)**. Nunca detectado porque o teste existente mockava `prisma.contact.delete` direto (não exercia a constraint contra Postgres real) e **não existe nenhum consumidor no frontend** chamando essa rota (confirmado por grep). Achado extra na mesma revisão: a rota não tinha `@Roles` — qualquer usuário autenticado podia excluir um contato, não só `ADMIN`. **Concluído 2026-07-25:** `DELETE /contacts/:id` deixou de apagar e passou a **anonimizar** — `name`→`"Contato removido"`, `phone`→`anonimizado-<id>` (evita colidir com o índice único `companyId+phone`), `email`/`avatarUrl`/`metadata` zerados, novo campo `anonymizedAt` (migration `20260725215606_contact_anonymization`) marca quando; idempotente (400 se já anonimizado); `update()` também passou a recusar editar um contato já anonimizado (evita ressuscitar PII por engano). Restrito a `@Roles(Role.ADMIN)`. Auditoria migrada de `contact.deleted` para `contact.anonymized`. **Validado contra Postgres real:** criado um contato de teste com uma conversa vinculada de verdade (via SQL direto, simulando histórico) — a chamada antiga teria quebrado com FK violation; a nova anonimiza com sucesso (200), a conversa permanece intacta, uma 2ª tentativa no mesmo contato recusa com 400, e a auditoria registra `contact.anonymized` com o `before` correto. Dados de teste removidos do banco depois. | `apps/api/prisma/schema.prisma` · `apps/api/prisma/migrations/20260725215606_contact_anonymization/` (novo) · `apps/api/src/modules/contact/contact.service.ts` · `apps/api/src/modules/contact/contact.controller.ts` · `apps/api/src/modules/contact/contact.service.spec.ts` · `docs/API_CONTRACT.md` | ✅ 2026-07-25 |
| B-29 | **[Descoberto junto com B-28/B-17, 2026-07-25]** Mídia de um contato anonimizado (fotos, áudios, documentos do WhatsApp) permanece no MinIO indefinidamente — `StorageService#delete` existe (`storage.service.ts`, usa `client.removeObject`) mas **não é chamado em nenhum lugar do código**. A URL pública do objeto (bucket com policy de leitura pública, ver F1-9/F6) continua válida mesmo depois que a PII do contato é removida do Postgres — o "esquecimento" hoje é só parcial (banco, não objeto). **Concluído 2026-07-25** (pedido do usuário: "pode seguir"). Novo `StorageService#deleteByUrl(url)` — a tabela `Attachment` só guarda a URL pública completa, não a key isolada que `client.removeObject` exige, então o método recorta o prefixo `publicUrl/bucket/` antes de deletar (rejeita URL fora do bucket configurado, em vez de tentar deletar algo às cegas). `ContactService#remove` (anonimização, B-28) ganhou o passo `purgeMedia`: busca todos os `Attachment` das conversas do contato (via `message.conversation.contactId`), apaga cada objeto do MinIO **e** a linha de `Attachment` — best-effort por item (uma falha isolada não desfaz a anonimização, que já aconteceu antes; fica registrada em `mediaPurged`/`mediaFailed` na auditoria e na resposta). **Validado contra MinIO+Postgres reais, não só mock:** subiu um objeto de verdade no bucket, criou contato+conversa+mensagem+`Attachment` apontando pra ele, anonimizou via API — o objeto **deixou de existir no MinIO** (`statObject` → `NotFound`), a linha de `Attachment` sumiu do Postgres, a conversa/mensagem continuam intactas, e a auditoria registra `mediaPurged:1, mediaFailed:0`. | `apps/api/src/shared/storage/storage.service.ts` (+ `storage.service.spec.ts`, novo) · `apps/api/src/modules/contact/contact.service.ts` · `apps/api/src/modules/contact/contact.service.spec.ts` | ✅ 2026-07-25 |
| B-30 | **[Descoberto junto com B-28/B-17, 2026-07-25]** Não existe endpoint de exportação/portabilidade de dados do titular (LGPD art. 18, incisos IV/V) — hoje, atender essa solicitação é 100% manual (`ADMIN` monta a resposta consultando `GET /contacts/:id` + `GET /conversations/:conversationId/messages` de cada conversa). **Concluído 2026-07-25** (pedido do usuário, escolhido entre os itens restantes do backlog). Novo `GET /contacts/:id/export` (`ADMIN+`): devolve `{ contact, conversations, exportedAt }` num único JSON — `contact` (+ `tags[]`), todas as conversas do titular **sem paginação** (é exportação pontual, não listagem de uso corrente) com `messages[]` completo em ordem cronológica (conteúdo, remetente, status, anexos). Recusa com 400 pra contato já anonimizado (não há mais PII pra devolver — mesma guarda de B-28). Auditado como `contact.exported` (novo — todo acesso de exportação fica registrado, não só as mutações). `docs/API_CONTRACT.md` atualizado no mesmo commit. **Validado contra Postgres real:** contato de teste com conversa+mensagem reais → export traz tudo certo (contato, conversa, mensagem, `exportedAt`); auditoria confirma `contact.exported` com o `userId` de quem exportou; anonimizado o mesmo contato em seguida → 2ª tentativa de export recusa com 400 (guarda funcionando de ponta a ponta). Dados de teste removidos do banco depois. | `apps/api/src/modules/contact/contact.service.ts` · `apps/api/src/modules/contact/contact.controller.ts` · `apps/api/src/modules/contact/contact.service.spec.ts` · `docs/API_CONTRACT.md` | ✅ 2026-07-25 |
| B-31 | **[Pedido do usuário, 2026-07-25]** "Criar as etapas de atendimento: fila de atendimento → meus → em atendimento e encerrados, e colocar as conversas que forem entrando na fila de atendimento até o responsável atender o chamado." O backend já tinha o pipeline inteiro pronto (`ConversationStatus`: WAITING/OPEN/RESOLVED/CLOSED; `assign()` já move WAITING→OPEN sozinho quando atribui um agente; webhook já cria conversa nova sempre como WAITING) — **o frontend nunca chamava `PATCH /conversations/:id/assign` nem `/status`, e não existia UI nenhuma pra sair da fila ou encerrar uma conversa.** Antes de implementar, confirmadas 3 decisões de design com o usuário: "Meus" = só minhas (OPEN + meu id) vs. "Em atendimento" = de todo mundo (OPEN de qualquer agente, útil pro supervisor); botão "Atender" na fila (não automático); botão "Encerrar" no chat (não só automático por timeout). **Concluído 2026-07-25.** `ConversationQueue.jsx` ganhou 4 abas (`queue-tabs`) — 3 delas filtram client-side a mesma lista ativa já carregada (`status`/`agentId`, sem round-trip extra); "Encerrados" busca sob demanda (`GET /conversations?status=CLOSED`, carregada só quando a aba é aberta pela 1ª vez, com paginação própria). Item da fila virou `<div role="button">` com um `<button>` "Atender" aninhado (`stopPropagation` — não dá pra aninhar `<button>` em `<button>`), que atribui a conversa a quem clicou e já abre ela. `ChatPanel.jsx` ganhou o botão "Encerrar" no cabeçalho (oculto se já `CLOSED`), com confirmação via `useConfirm` (B-13, ação terminal — CLOSED não volta pra WAITING/OPEN no backend). `api.js`: `getConversations` passou de posicional pra `{page,limit,status,agentId}`, e ganhou `assignConversation`/`updateConversationStatus` (endpoints já existiam e já estavam documentados em `docs/API_CONTRACT.md` — só nunca tinham consumidor). **Achado no caminho:** a fixture mock `conv-4` estava com status `RESOLVED` — um status que o backend suporta mas que nenhuma ação do produto (nem antes, nem depois deste item) jamais seta; trocada pra `CLOSED` pra não virar um item invisível em todas as 4 abas novas. **Validado com Playwright real** contra o front em modo mock (instância própria na porta 3010, sem tocar a instância real do usuário rodando na 3000 — que tem conversa real de WhatsApp com cliente real e não deveria ser mexida por teste): Atender remove da fila e abre o chat; "Meus"/"Em atendimento" mostram a atribuição corretamente com o nome do agente; Encerrar fecha o chat e a conversa aparece em "Encerrados". Achado e corrigido no caminho: o 1º teste apontou 2 "bugs" que eram só timing do meu próprio script (esperei 500ms, o mock simula 800ms de latência) — refeito com espera adequada, confirmado que a feature sempre funcionou certo. | `src/components/ConversationQueue.jsx` · `src/components/ConversationQueue.test.js` · `src/components/ChatPanel.jsx` · `src/components/ChatPanel.test.js` · `src/hooks/useConversations.js` · `src/hooks/useConversations.integration.test.js` · `src/main.js` · `src/services/api.js` · `src/services/apiMock.js` · `src/data/mockConversations.js` · `src/styles.css` | ✅ 2026-07-25 |
| B-32 | **[Pedido do usuário, 2026-07-26]** Dashboard com dados reais no lugar do botão "Funis" (placeholder desabilitado desde sempre, nunca teve tela) — vira a **1ª tela do usuário**, antes da Caixa de Entrada, na mesma ordem dos botões do menu lateral. Inspirada na dashboard do TIFLUX: chamados atendidos vs. não atendidos, soluções resolvidas vs. não resolvidas. **Decisão do usuário sobre "resolvida"/"não resolvida":** perguntar ao atendente no momento de Encerrar (não reaproveitar o `RESOLVED` do enum de status, que nunca é setado por nenhuma ação real do produto, nem tratar todo encerramento como resolvido automaticamente). **Concluído 2026-07-26.** Backend: novo enum `ConversationResolution` (`RESOLVED`/`UNRESOLVED`) + campo `Conversation.resolution` (migration `20260726143923_conversation_resolution`); `PATCH /conversations/:id/status` passou a exigir `resolution` só quando `status=CLOSED` (`@ValidateIf`, class-validator). Novo módulo `dashboard/` com `GET /dashboard/summary?from=&to=` (Prisma `count` puro, sem lib nova) — `attended`/`notAttended` por `createdAt` no período (teve agente vs. não), `resolved`/`unresolved`/`unlabeled` por `closedAt` no período (`unlabeled` = encerradas antes deste campo existir, contada à parte pra não distorcer o dado real). Frontend: `recharts` (única lib nova, React 19-compatível nativamente) desenha 2 donuts; `DashboardView.jsx` novo, vira a view inicial (`main.js`, `useState("dashboard")` no lugar de `"inbox"`); `Sidebar.jsx`: "Funis" → "Dashboard" como 1º item do menu, "Relatórios" ganhou o ícone `clipboard` (antes dividia o `chart` com o que virou Dashboard). `ChatPanel.jsx`: botão "Encerrar" agora abre `CloseConversationDialog.jsx` (novo, modal dedicado com 2 ações primárias — não é o `useConfirm` binário do B-13, que não serve pra uma escolha de 3 vias) pedindo Resolvido/Não resolvido/Cancelar antes de fechar. **Validado com Playwright real** (instância mock isolada, porta 3013 — nunca a real do usuário): Dashboard carrega como 1ª tela com números reais, menu na ordem certa sem "Funis", diálogo de encerramento com o motivo funciona ponta a ponta, e os números do dashboard refletem o encerramento imediatamente ao voltar pra tela. Achado e descartado no caminho: um "bug" de donut sem fatias era só timing do meu script de QA (mesma classe de falso positivo já registrada no changelog do B-31), confirmado reproduzindo com espera maior. **Ampliação no mesmo dia** (pedido do usuário: "preciso que seja algo pararelo, utilizando as ferramentas que já temos", colando o HTML real do dashboard do TIFLUX como referência): comparei widget a widget — vários conceitos do TIFLUX não existem no produto (horas apontadas do atendente, avaliação/CSAT de ticket — já decidido não ter, compromissos de agenda, atividades agendadas, pré-tickets) e não foram inventados. Decisão do usuário entre as opções apresentadas: adicionar **"Resumo de conversas"** (Todas/Minhas/Não atendidas/Em atendimento/Vencidas por SLA) e **"Sem resposta do atendente"** (Todas/Minhas — última mensagem é do cliente e ninguém respondeu ainda), mantendo grade fixa de cards (sem `react-grid-layout`/Ant Design, que o TIFLUX usa — não são ferramentas que já tínhamos). `GET /conversations/stats` (B-2) ganhou 4 campos novos (`myOpen`, `slaBreached`, `awaitingReply`, `myAwaitingReply`) — "minhas" sempre vem do JWT do requisitante, nunca de um parâmetro; "sem resposta" precisou olhar a ÚLTIMA mensagem de cada conversa OPEN (`findMany` com `messages: { take: 1, orderBy: sentAt desc }`, resolvido pelo Postgres como um único lateral join, não N+1). `DashboardView.jsx` ganhou 2 seções novas de cards (mesmo componente `StatRow` reaproveitado), deixando claro na UI que são dados em **tempo real** ("agora"), diferente dos cards por período (últimos 30 dias) que já existiam. | `apps/api/prisma/schema.prisma` · `apps/api/prisma/migrations/20260726143923_conversation_resolution/` (novo) · `apps/api/src/modules/conversation/conversation.service.ts` · `apps/api/src/modules/conversation/conversation.controller.ts` · `apps/api/src/modules/conversation/conversation.service.spec.ts` · `apps/api/src/modules/conversation/dto/update-conversation-status.dto.ts` · `apps/api/src/modules/dashboard/` (novo) · `apps/api/src/app.module.ts` · `docs/API_CONTRACT.md` · `src/components/DashboardView.jsx` (novo) · `src/components/DashboardView.test.js` (novo) · `src/components/CloseConversationDialog.jsx` (novo) · `src/components/ChatPanel.jsx` · `src/components/ChatPanel.test.js` · `src/components/Sidebar.jsx` · `src/components/Sidebar.test.js` · `src/main.js` · `src/services/api.js` · `src/services/apiMock.js` · `src/data/mockConversations.js` · `src/hooks/useConversations.js` · `src/hooks/useConversations.integration.test.js` · `src/styles.css` · `package.json` (recharts) | ✅ 2026-07-26 |
| B-33 | **[Pedido do usuário, 2026-07-26]** Gerador de relatórios: base de atendimento, tipos de atendimento e por atendente, com exportação em CSV **e PDF**. **Concluído 2026-07-26.** 3 relatórios escolhidos pelo usuário entre 6 sugeridos (ficaram de fora: por departamento/fila e SLA — podem virar item novo depois). Backend: módulo `report/` novo, `GET /reports/attendance` (lista detalhada por conversa), `GET /reports/by-tag` (volume/tempo médio por tag — uma conversa com 2 tags conta nas 2, categorias não-exclusivas), `GET /reports/by-agent` (atendidas/resolvidas/não resolvidas/em aberto/tempo médio por agente ativo) — todos com `?from=&to=&format=json|csv|pdf`, mesmo período padrão de 30 dias do B-32. `format=csv` gera na hora (sem lib, `text/csv` com BOM pro Excel abrir acentuação certa); `format=pdf` usa **PDFKit** (novo, só Node — sem Chromium/Puppeteer, mesma decisão de manter a imagem Docker leve já tomada no B-33 original) desenhando tabela célula a célula com paginação manual. **Bug real encontrado e corrigido no caminho:** `import PDFDocument from 'pdfkit'` compilava para `pdfkit_1.default` e quebrava em runtime ("is not a constructor") mesmo com `tsc`/`nest build` limpos — o tsconfig do projeto não tem `esModuleInterop`, só `allowSyntheticDefaultImports` (que relaxa só a checagem de TIPO, não muda o JS emitido); corrigido com `import * as PDFDocument`, mesmo padrão já usado pro `compression` em `main.ts`. Só foi pego porque validei a geração de PDF de verdade fora da suíte automatizada (a suíte de `ReportService` só testava os formatadores, nunca chamava `sendPdf`) — depois de corrigir, um `report-export.util.spec.ts` novo passou a cobrir isso de verdade (`sendCsv`/`sendPdf` executados com uma stream `Writable` real, não um mock incompleto). Frontend: `ReportsView.jsx` novo — seletor de tipo (dropdown, mesmo padrão do B-31), 2 campos de data nativos (`<input type="date">`, sem lib), tabela genérica orientada a config de colunas por relatório, botões "Exportar CSV"/"Exportar PDF" que baixam o arquivo via Blob (`URL.createObjectURL` + `<a>` temporário, sem lib). PDF não é suportado em modo demonstração (mock não replica PDFKit) — erro amigável em vez de fingir que funciona. "Relatórios" deixou de ser "em breve" no menu lateral. **2º bug real encontrado testando o relatório "por atendente":** `apiMock.js#getReport('attendance', ...)` tratava `c.contact` como string, mas o mock guarda o shape bruto do contrato da API (`{id,name,phone,avatarUrl}`) — corrigido pra `c.contact?.name || c.contact?.phone`. **3º achado (inconsistência de dado só do mock, não bug de código):** `mockUsersList` (Administrador/Agente 1/Supervisor, ids `user-N`) sempre foi uma lista totalmente separada dos agentes já referenciados nas conversas fixture (Camila/Nando/Juliana, ids `agent-N`) — o relatório "por atendente" em modo demo sempre dava vazio por causa disso (e "Minhas" no dashboard/fila também, silenciosamente, desde sempre). Corrigido adicionando os 3 agentes fixture a `mockUsers`, sem tocar nas conversas — bônus: "Atender" em modo demo agora também consegue atribuir a eles. | `apps/api/src/modules/report/` (novo) · `apps/api/package.json` (pdfkit) · `apps/api/src/app.module.ts` · `docs/API_CONTRACT.md` · `src/components/ReportsView.jsx` (novo) · `src/components/ReportsView.test.js` (novo) · `src/components/Sidebar.jsx` · `src/components/Sidebar.test.js` · `src/main.js` · `src/services/api.js` · `src/services/api.test.js` · `src/services/apiMock.js` · `src/styles.css` | ✅ 2026-07-26 |
| B-34 | **[Pedido do usuário, 2026-07-26]** Base de administração de contatos no frontend — listar, buscar e cadastrar novo contato pela plataforma (hoje só existe via webhook do WhatsApp). Backend já tem CRUD completo (`GET/POST/PATCH/DELETE /contacts`, `contact.controller.ts`) sem nenhum consumidor no front — mesma classe de gap que motivou B-8/B-27 (endpoint pronto, zero UI). | `src/pages/ContactsPage.jsx` (novo) · `src/services/api.js`/`apiMock.js` · `src/components/Sidebar.jsx` | 🔲 planejado |
| B-35 | **[Pedido do usuário, 2026-07-26]** Auto-atendimento configurável em Configurações: mensagem de saudação, menu de opções (estilo URA de empresas), menu de inatividade (do atendente/sistema, não do contato), menu de encerramento, horário de atendimento (resposta automática fora do expediente). Maior item novo em superfície — precisa de modelo de dados novo (fluxo/menu por empresa), execução no webhook da Evolution, e estado de sessão por conversa (Redis, já usado no projeto via `ioredis`/Bull). | `apps/api/prisma/schema.prisma` (novo modelo) · `apps/api/src/modules/webhook/` · `apps/api/src/modules/auto-attendance/` (novo) · `src/components/settings/SettingsPanel.jsx` | 🔲 planejado |

---

## 🧭 Registro de decisões

| Data | Decisão | Motivo | Itens afetados |
|---|---|---|---|
| 2026-07-25 | B-19: manter tokens em `localStorage` (não migrar pra cookie `httpOnly`+CSRF por agora) — decisão consciente, não default por inércia. **Gatilho pra reabrir:** qualquer XSS real encontrado no produto, dependência com CVE de XSS confirmada, ou proximidade real do 1º cliente pagante (o que vier primeiro). | Risco de XSS hoje é baixo (nenhum `dangerouslySetInnerHTML` sobre conteúdo de usuário/mensagem, conferido em todo `src/`) — o custo de migrar (reescrever WebSocket auth, adicionar CSRF, retestar todo o fluxo de login/refresh/logout) não se paga ainda. Produção já está bem posicionada pra migrar rápido quando o gatilho aparecer (subdomínios same-site, CORS já pronto pra credentials cross-origin). | B-19 |
| 2026-07-25 | B-28: exclusão de contato (LGPD) vira **anonimização**, não hard delete — registro e histórico de conversas permanecem, só a PII (`name`/`phone`/`email`/`avatarUrl`/`metadata`) é substituída/zerada. | LGPD art. 16 permite reter dado após pedido de exclusão quando há motivo legítimo pra retenção, desde que anonimizado — aqui, o histórico de atendimento é registro de negócio da empresa (inclui a resposta do próprio atendente), não só dado do titular. Hard delete também não era viável tecnicamente sem uma reformulação maior do schema (a relação `Conversation.contact` não tem cascade de propósito, é histórico legítimo). | B-28, B-17 |
| 2026-07-25 | B-26: recuperação de senha no modelo **reset administrativo mínimo** — endpoint `ADMIN`-only que gera senha temporária sem e-mail, em vez de fluxo self-service por SMTP. Limitação aceita conscientemente: não resolve o `ADMIN` único da empresa se trancar sozinho fora dela. | Decisão do usuário ao escolher entre as duas opções levantadas no item: menor escopo, sem nova dependência externa (provedor SMTP) nem infraestrutura de e-mail nova no projeto. Se o caso do `ADMIN` único trancado se mostrar real em produção, reabrir como item novo (fluxo por e-mail). | B-26 |
| 2026-07-25 | B-9: onboarding de tenant no modelo **auto-cadastro público** — o próprio contratante cria a empresa e vira o `ADMIN` (não `SUPER_ADMIN`) daquela empresa no mesmo fluxo, sem aprovação manual. **Pré-requisito descoberto no caminho:** e-mail precisa virar único globalmente (hoje só é único por empresa — ver B-20), porque o login não pede empresa, só e-mail+senha. | Decisão do usuário: "projeto web, o ideal seria auto-cadastro público, onde o contratante cria a empresa e o primeiro login é do admin controlador daquela empresa" — modelo self-service, sem fricção comercial manual, alinhado ao padrão de produtos SaaS web (Slack/Notion). `SUPER_ADMIN` (papel multi-empresa) fica de fora do fluxo — nem é criável via API hoje (`user.service.ts:120-123`) e não é necessário pro primeiro admin de uma empresa nova. | B-9, B-20 |
| 2026-07-24 | B-5: remover o container `minio_init` por completo em vez de só atualizar a tag quebrada | A tag `minio/mc:RELEASE.2025-06-13T11-33-47Z` já não existe no Docker Hub, mas o container era redundante independente disso — `StorageService#onModuleInit` já faz exatamente o mesmo trabalho (`makeBucket` + `setBucketPolicy` de leitura pública) no boot da própria API. Atualizar a tag só adiaria o problema pro dia em que ela também sair do Hub; remover elimina a causa | B-5 |
| 2026-07-15 | Padronizar nomes de eventos socket no formato do backend (`message.new`, `conversation.updated`, …) | O backend é a fonte da verdade do contrato; mudar só o front evita tocar em 13 pontos de emissão | F0-3, F0-4 |
| 2026-07-15 | Envio de mensagem sempre via REST; socket apenas para receber eventos | O gateway não tem handler de envio; o REST passa por DTO validado + regras de permissão. (Corrigido em 2026-07-15: o envio ao WhatsApp é **síncrono** — a fila `message-send` não participa do fluxo; ver B-6) | F0-4, B-6 |
| 2026-07-15 | Seed não tenta mais auto-deletar o arquivo de credenciais | O `setTimeout` de 1h prendia o processo (`db:seed` nunca terminava) e nunca funcionaria de fato (morre com o processo). O arquivo é gitignored e a instrução de deleção manual permanece | F0-7 |
| 2026-07-16 | Ícones da UI: svgrepo.com é a fonte padrão (coleção Tabler Icons); emojis banidos da interface | Pedido do usuário — visual profissional e consistente entre SO/navegadores; componente central `Icon` facilita manutenção | F8-11 |
| 2026-07-16 | `user:typing` removido do frontend (em vez de implementar ponta a ponta) | O gateway não tem handler, o servidor nunca emite o evento e nenhum componente chamava `setTyping` — era código morto nos dois sentidos. Reintroduzir ponta a ponta se o produto pedir indicador de digitação | F2-5 |
| 2026-07-16 | Erros da API sempre via classe `ApiError` (com `.status`/`.message`); rede detectada por `instanceof TypeError` | Objeto literal lançado quebrava `error.message.includes` quando `message` era `undefined`, mascarando o erro original | F1-5, F2-1 |
| 2026-07-16 | Frontend permanece **Vite + React (JS)** — sem migração para Next.js/TS agora | A stack atual está estável (74 testes, build ok, integração real validada); migrar no meio da estabilização custaria semanas sem valor ao usuário. Reavaliar apenas se surgir necessidade concreta (SSR, SEO) | F5-3 |
| 2026-07-21 | `register()` removido do front (não implementado no back) | Rota `POST /auth/register` nunca existiu no backend e nenhuma tela chama `register()` — era código morto sem consumidor real. Reintroduzir só se o produto pedir auto-cadastro | F1-4 |
| 2026-07-21 | Métricas do dashboard ficam restritas ao que é calculável do lado do front hoje (contagens por status + não lidas); "Primeira resposta" e "Satisfação" removidas em vez de mantidas fictícias | Não existe CSAT nem agregação de tempo de resposta no schema/backend — mostrar número fixo/fictício nesses cards seria pior que não mostrar. Endpoint agregado fica no backlog (B-2) | F1-2, B-2 |
| _pendente_ | Tokens: cookie httpOnly+CSRF ou manter localStorage com mitigações? | — | F6-1 |
| 2026-07-26 | B-32: "resolvida" x "não resolvida" vira uma pergunta no fluxo de **Encerrar** — o atendente escolhe o motivo (resolvido/não resolvido) antes da conversa fechar, em vez de todo encerramento contar automaticamente como resolvido ou de reintroduzir `RESOLVED` como etapa própria no meio da máquina de status. | Decisão do usuário entre as 3 opções apresentadas — dado mais preciso pro dashboard/relatórios, sem mexer na máquina de status `WAITING→OPEN→CLOSED` já testada e validada em B-31. | B-32 |

---

## 📝 Changelog

> Formato: `AAAA-MM-DD — resumo (itens tocados) — evidência`

| Data | O que foi feito | Itens | Evidência |
|---|---|---|---|
| 2026-07-26 | **B-33 — gerador de relatórios: base de atendimento, por tipo/tag e por atendente, com exportação CSV/PDF (pedido do usuário, 3 relatórios escolhidos entre 6 sugeridos).** Backend: módulo `report/` novo (`GET /reports/attendance`\|`by-tag`\|`by-agent`, todos com `?from=&to=&format=json\|csv\|pdf`). PDF via **PDFKit** (só Node, sem Chromium) — **bug real corrigido no caminho**: `import PDFDocument from 'pdfkit'` compilava pra `pdfkit_1.default` e quebrava em runtime mesmo com o build limpo (tsconfig sem `esModuleInterop`); só apareceu porque validei a geração de PDF de verdade fora da suíte (que só testava os formatadores) — `report-export.util.spec.ts` novo cobre isso agora com uma stream real. Frontend: `ReportsView.jsx` novo (dropdown de tipo + datas nativas + tabela + exportação via Blob), "Relatórios" saiu de "em breve". **2 achados extras testando com dado real do mock:** `getReport('attendance')` tratava `contact` como string quando é o objeto bruto do contrato (corrigido); e `mockUsersList` sempre foi uma lista desconectada dos agentes já citados nas conversas fixture (Camila/Nando/Juliana nunca existiam como usuário de verdade no mock) — corrigido, bônus de "Atender" em demo passar a funcionar com eles também. | B-33 | Backend: `npm test` 26 suites, **221/221** (19 novos: `report.service.spec.ts` com 15, `report-export.util.spec.ts` novo com 4) · `npm run build` limpo · `npm audit --omit=dev`: 3 moderadas (mesma raiz uuid/bull de sempre, pdfkit não trouxe nada novo). Frontend: `npm test` 26 suites, **227/227** (14 novos: `ReportsView.test.js` novo com 6, 8 em `api.test.js`, 1 em `Sidebar.test.js`) · `npm run build` limpo · Playwright real (instância mock isolada, porta 3016, nunca a real do usuário) confirmando os 3 relatórios com dado real, download de CSV de verdade capturado pelo evento `download` do Playwright, e erro amigável de PDF em modo demo |
| 2026-07-26 | **Ajuste visual do B-32 (pedido do usuário: "preciso remover os textos em small que foram aplicados nas telas, também o resumo de conversar, quero que tire do dashboard e deixe em apenas nas mensagens").** Removido o texto secundário (`<small>`) de todos os cards de indicador — `Metrics.jsx` (Caixa de Entrada) e `DashboardView.jsx` (Dashboard), inclusive a classe CSS `.metric-card small`, agora morta, removida junto. "Resumo de conversas (agora)" saiu do Dashboard — o mesmo retrato em tempo real já existe na Caixa de Entrada via `Metrics.jsx`, então passou a viver só lá, sem duplicar; "Sem resposta do atendente" permanece no Dashboard (não tem equivalente na Caixa de Entrada). Backend não mudou — `GET /conversations/stats` continua retornando `myOpen`/`slaBreached` (consumidos só pela Caixa de Entrada agora, não mais pelo Dashboard). | B-32 | `npm test` (front): 25 suites, **213/213** (2 novos: 1 em `Metrics.test.js`, 1 em `DashboardView.test.js`, confirmando ausência de `<small>`/da seção removida) · `npm run build` limpo · Playwright real (instância mock isolada, porta 3015) confirmando visualmente os 2 telas (Dashboard e Caixa de Entrada) sem os textos secundários e sem duplicação do resumo |
| 2026-07-26 | **Ampliação do B-32 — dashboard ganha widgets em tempo real, comparado direto contra o dashboard real do TIFLUX (pedido do usuário: "preciso que seja algo pararelo, utilizando as ferramentas que já temos", com o HTML do TIFLUX colado como referência).** Mapeamento widget a widget: vários conceitos do TIFLUX (horas apontadas, avaliação/CSAT, compromissos de agenda, atividades agendadas, pré-tickets) não existem no schema do AtendeHub e não foram inventados — só o que dava pra alimentar com dado real entrou. Decisão do usuário (2 perguntas, entre as opções apresentadas): **"Resumo de conversas"** (Todas/Minhas/Não atendidas/Em atendimento/Vencidas por SLA) + **"Sem resposta do atendente"** (Todas/Minhas), com **grade fixa de cards** (não a grade arrastável/redimensionável do TIFLUX — exigiria `react-grid-layout` + Ant Design, fora do stack atual). `GET /conversations/stats` ganhou `myOpen`/`slaBreached`/`awaitingReply`/`myAwaitingReply` — "minhas" sempre do JWT do requisitante (nunca um parâmetro livre, evita vazar contagem de outro agente); "sem resposta" identifica a última mensagem de cada conversa OPEN via `findMany` com `messages: {take:1, orderBy: sentAt desc}` (1 query só, sem N+1). | B-32 | Backend: `npm test` 24 suites, **202/202** (7 novos em `conversation.service.spec.ts`) · `npm run build` limpo. Frontend: `npm test` 25 suites, **211/211** (1 novo em `DashboardView.test.js`) · `npm run build` limpo · Playwright real (instância mock isolada, porta 3014, nunca a real do usuário) confirmando visualmente as 2 seções novas com dado real do mock, layout comparável ao TIFLUX (cards em fileira + donuts) sem nenhuma dependência nova além do `recharts` já instalado |
| 2026-07-26 | **B-32 — Dashboard com dados reais, 1ª tela do usuário (pedido do usuário: novo roadmap de produto, "pode implementar").** No lugar de "Funis" (nunca teve tela), inspirada no TIFLUX: chamados atendidos/não atendidos, soluções resolvidas/não resolvidas. Decisão prévia do usuário (entre 3 opções apresentadas): motivo do encerramento vira uma escolha obrigatória no botão "Encerrar" (`CloseConversationDialog.jsx` novo — Resolvido/Não resolvido/Cancelar), não um status automático nem o `RESOLVED` do enum (que nunca era setado por nenhuma ação real). Backend: campo novo `Conversation.resolution` + `PATCH .../status` exige `resolution` só quando fecha (`@ValidateIf`); módulo `dashboard/` novo com `GET /dashboard/summary` (Prisma `count`, sem lib nova — `attended`/`notAttended` por `createdAt` no período, `resolved`/`unresolved`/`unlabeled` por `closedAt`). Frontend: `recharts` (única dependência nova, compatível com React 19 nativamente) desenha 2 donuts; `DashboardView.jsx` novo vira a view inicial (`main.js`), `Sidebar.jsx` reordenado (Dashboard 1º item). | B-32 | Backend: `npm test` 24 suites, **199/199** (18 novos: `dashboard.service.spec.ts` novo com 7, 2 em `conversation.service.spec.ts`) · `npm run build` limpo · migration `20260726143923_conversation_resolution` aplicada contra Postgres real. Frontend: `npm test` 25 suites, **210/210** (11 novos: `DashboardView.test.js` novo com 4, 2 em `ConversationQueue`→`ChatPanel.test.js`, 2 em `Sidebar.test.js`) · `npm run build` limpo · `npm audit --omit=dev`: 0 vulnerabilidades (as 20 do audit geral são só devDependencies do próprio Jest, nada que vá pro bundle) · Playwright real (instância mock isolada, porta 3013, nunca a real do usuário) confirmando visualmente: dashboard como 1ª tela com números reais, menu sem "Funis", diálogo de encerramento ponta a ponta, números atualizando ao vivo após encerrar |
| 2026-07-26 | **Correção do B-31 — seletor de etapas virou dropdown + paginação passou a ser por aba (2 pedidos do usuário sobre o mesmo item: "o css quebrou de novo... mantenha as opções no menu central... faça o scroll infinito de acordo com a quantidade de conversas que vão aparecendo").** As 4 abas em botões (`role="tab"`) quebravam o layout estreito da coluna da fila mesmo depois de um 1º ajuste de espaçamento (`flex-wrap`) — trocadas por um `<select>` único (`queue-tab-select`), sempre 1 linha, mostrando a etapa atual com contagem (ex.: "Fila de atendimento (2)"), escolha confirmada pelo usuário entre as opções apresentadas. Problema mais profundo por baixo do CSS: "Fila"/"Meus"/"Em atendimento" dividiam uma única paginação genérica (`loadMoreConversations`, sem filtro de `status`/`agentId`) — o scroll infinito buscava "mais conversas" no geral, não "mais conversas desta etapa", ficando impreciso quando o volume real de cada etapa crescia. `useConversations.js` ganhou 3 pares de paginação independentes (`loadMoreQueueConversations`/`queueHasMore`/`queueLoadingMore` para WAITING, `loadMoreMineConversations`/`mineHasMore`/`mineLoadingMore` para OPEN+`agentId` do usuário logado, `loadMoreOpenConversations`/`openHasMore`/`openLoadingMore` para OPEN geral), cada um chamando `apiClient.getConversations({status, agentId, page, limit})` com o filtro certo da aba e fundindo o resultado no mesmo array `conversations` (dedupe por id) — mesmo padrão que "Encerrados" já usava. Hook passou a receber `currentUserId` como parâmetro (`useConversations(user?.id)` em `main.js`) para poder filtrar "Meus" no servidor. | B-31 | `npm test`: 24 suites, **203/203** (6 novos: `ConversationQueue.test.js`+2, `useConversations.integration.test.js`+4, substituindo os 3 testes do describe antigo "scroll infinito da fila") · `npm run build` limpo · Playwright real (Chromium, instância mock isolada na porta 3012, sem tocar a instância real do usuário na 3000) confirmando visualmente as 4 opções do dropdown em 1 linha só (1400px e 900px de largura), filtragem correta em cada uma, e disparo do `loadMore` certo por aba ao rolar a lista |
| 2026-07-25 | **B-31 — etapas de atendimento no frontend: Fila de atendimento → Meus → Em atendimento → Encerrados (pedido do usuário, com 3 decisões de design confirmadas antes de implementar).** O backend já tinha o pipeline pronto (WAITING/OPEN/RESOLVED/CLOSED, `assign()` já move pra OPEN sozinho) — só nunca tinha consumidor no frontend (`assign`/`status` nunca eram chamados). `ConversationQueue.jsx` ganhou 4 abas: 3 filtram client-side a lista ativa já carregada (`status`/`agentId`), "Encerrados" busca sob demanda no servidor (`status=CLOSED`) com paginação própria. Botão "Atender" na fila (atribui + abre a conversa); botão "Encerrar" no chat (confirmação via `useConfirm`, ação terminal). `api.js#getConversations` passou de posicional pra `{page,limit,status,agentId}`; novos `assignConversation`/`updateConversationStatus`. Achado no caminho: fixture mock `conv-4` em status `RESOLVED` (nunca usado por nenhuma ação real do produto) virava item invisível em todas as 4 abas novas — trocada pra `CLOSED`. **Validado com Playwright real** numa instância de mock isolada (porta 3010) — deliberadamente sem tocar a instância real do usuário (porta 3000), que tinha uma conversa de WhatsApp real em andamento com um cliente real. | B-31 | `npm test`: 24 suites, **199/199** (12 novos: `ConversationQueue.test.js`+5, `ChatPanel.test.js`+4, `useConversations.integration.test.js`+3) · `npm run build` limpo · Playwright real (Chromium, instância isolada) confirmando visualmente Atender/Meus/Em atendimento/Encerrar/Encerrados, com timing corrigido depois de um falso positivo do próprio script de teste (800ms de delay simulado do mock vs. 500ms de espera insuficiente) |
| 2026-07-25 | **Revisão de código completa do frontend antes de subir pra uso real (pedido do usuário) + QA visual + checklist de prontidão de deploy.** Revisão manual dos 34 arquivos-fonte do front achou 2 bugs reais em código que roda hoje (`api.js`: sessão expirada fazia `window.location.href='/login'` — hard reload numa rota que não existe nesta SPA sem router, em vez do padrão de logout via estado React usado no resto do app; `websocket.js`: flag `refreshingToken` travava em `true` pra sempre se o refresh falhasse durante reconexão do socket) e 3 arquivos de código morto com bugs latentes (`useFetch.js`, `ProtectedRoute.jsx`, `ContextItem.jsx` — zero imports, removidos). QA visual com Playwright real cobriu login→conversa→mensagem→notificações→7 seções de Configurações→logout sem nenhum erro de console. Checklist de deploy: build real das 2 imagens de produção (backend testado contra Postgres/Redis reais com `NODE_ENV=production`, login funcionando; frontend com env vars reais embutidas no bundle) — identificado que falta certificado SSL/domínio real antes de um deploy de produção de verdade (não existiam ainda). Sem servidor/domínio disponível, decisão do usuário: rodar em modo dev normal localmente (API+front cada um na sua janela, infra via `docker compose`) em vez do build de produção. | — | `npm test` front: 24 suites, **199/199** · Playwright real cobrindo os 2 fixes + QA visual completo · `docker build` real das imagens de API e nginx+frontend, testadas contra Postgres/Redis reais · commit `21f3320` |
| 2026-07-25 | **DSNs reais do Sentry configurados e validados de ponta a ponta (B-18) + B-19 decidido (reabertura pedida pelo usuário, análise concreta contra o código, decisão: manter `localStorage`).** Backend: `SENTRY_DSN` real em `apps/api/.env` (gitignorado) — evento de teste enviado através do `SentryExceptionFilter` de verdade (`Sentry.flush()` confirmou entrega). Frontend: `VITE_SENTRY_DSN` real em `.env` da raiz — rebuild confirmou o DSN embutido no bundle estático, dev server subido, e um erro genuíno disparado num Chromium real via Playwright gerou requisição `POST` real pro `ingest.us.sentry.io` com resposta **200**. B-18 agora está funcionando de verdade em ambiente local, não só implementado. B-19: análise concreta (não genérica) mostrou que a produção já roda subdomínios same-site (`app.`/`api.atendehub.com`) e CORS já pronto pra cookie cross-origin — o retrabalho real seria só o handshake do WebSocket (`websocket.js:41`/`events.gateway.ts:65-68` leem o token do `localStorage`); risco de XSS hoje é baixo (nenhum `dangerouslySetInnerHTML` sobre dado de usuário em todo `src/`, conferido). Decisão registrada com gatilho claro pra reabrir (XSS real, CVE de XSS numa dependência, ou proximidade do 1º cliente pagante). **Backlog do produto 100% fechado — B-1 a B-30 todos concluídos ou decididos conscientemente.** | B-18, B-19 | `Sentry.flush()` (backend) → `true` · Playwright real capturando `POST .../envelope/` → `200` do ingest do Sentry (frontend) · `.env`/`apps/api/.env` confirmados gitignorados (`git check-ignore`) · leitura direta de `websocket.js`/`events.gateway.ts`/`icons.jsx` sustentando a decisão de B-19 |
| 2026-07-25 | **B-18 — monitoramento de erros com Sentry (decisão do usuário: SaaS, tier gratuito, entre as opções apresentadas).** Backend: `SentryExceptionFilter` estende o `BaseExceptionFilter` do Nest (resposta HTTP idêntica a antes, validado via `curl`) e só captura exceções inesperadas — 5xx / não-`HttpException`; validação/404/409 nunca vira evento. Frontend: `initMonitoring()` no boot + `ErrorBoundary` (B-12) reportando pro Sentry, não só console. Sem DSN configurado (backend `SENTRY_DSN`, frontend `VITE_SENTRY_DSN`), o SDK não inicializa — confirmado subindo a API real sem a variável, boot e respostas de erro idênticos. `beforeSend` nos dois lados remove `request.data`/`cookies` do evento — mesma cautela de privacidade do LGPD (B-17/B-28/B-29/B-30), nunca manda telefone/conteúdo de mensagem pro Sentry. `VITE_SENTRY_DSN` wireado como build arg do `nginx` pra funcionar em produção de verdade (não só documentado). | B-18 | `npm test` backend: 23 suites, **191/191** (3 novos em `sentry.spec.ts`, novo) · `npm test` frontend: 23 suites, **184/184** (1 novo em `ErrorBoundary.test.js`) · `npm run build`/`lint:check` limpos nos dois lados · API real subida sem `SENTRY_DSN` → boot normal, `curl` em 404/400/200 idênticos a antes do filtro · `docker compose config` validando a interpolação do novo `VITE_SENTRY_DSN` |
| 2026-07-25 | **B-29 — purga de mídia do MinIO ao anonimizar um contato (pedido do usuário: "pode seguir").** `StorageService#deleteByUrl` novo — recorta o prefixo `publicUrl/bucket/` da URL guardada em `Attachment.url` (a tabela não guarda a key isolada) e delega pro `delete(key)` já existente, que nunca era chamado por ninguém. `ContactService#remove` (a anonimização do B-28) ganhou o passo `purgeMedia`: acha todos os `Attachment` das conversas do contato e apaga cada um do MinIO + a linha no Postgres, best-effort por item (uma falha isolada não desfaz a anonimização nem trava as demais purgas — fica em `mediaPurged`/`mediaFailed`, na auditoria e na resposta). **Validado contra MinIO+Postgres reais:** objeto de teste de verdade no bucket, contato+conversa+mensagem+`Attachment` apontando pra ele, anonimizado via API — objeto sumiu do MinIO (`statObject`→`NotFound`), `Attachment` sumiu do Postgres, conversa/mensagem preservadas, auditoria com `mediaPurged:1`. | B-29 | `npm test` (backend): 22 suites, **188/188** (4 novos: 2 em `storage.service.spec.ts`, novo, 2 em `contact.service.spec.ts`) · `npm run build`/`lint:check` limpos · objeto real subido no MinIO via script auxiliar, anonimização via `curl`, `statObject` confirmando `NotFound` depois |
| 2026-07-25 | **B-30 — endpoint de exportação/portabilidade de dados do titular (pedido do usuário: "vamos continuar", escolhido entre os itens restantes do backlog).** Novo `GET /contacts/:id/export` (`ADMIN+`): `{ contact, conversations, exportedAt }` num JSON só — contato completo (+`tags[]`) e todas as suas conversas **sem paginação** (exportação pontual, não listagem corrente), cada uma com `messages[]` cronológico completo (conteúdo, remetente, status, anexos). Recusa com 400 pra contato já anonimizado (mesma guarda do B-28 — não há mais PII pra devolver). Auditado como `contact.exported` (novo tipo de evento — primeiro caso no projeto de auditar um *acesso* de leitura sensível, não só mutação). | B-30 | `npm test` (backend): 21 suites, **184/184** (3 novos em `contact.service.spec.ts`) · `npm run build`/`lint:check` limpos · `curl` real (Postgres): contato+conversa+mensagem de teste → export traz tudo certo, auditoria confirma `contact.exported`, contato anonimizado em seguida → 2ª tentativa de export recusa com 400 |
| 2026-07-25 | **B-17 — política de retenção/LGPD documentada + bug real corrigido no caminho (pedido do usuário: "continue a partir do B-17").** Nova seção "Retenção de dados e LGPD" em `docs/RUNBOOK.md`: tabela de retenção (tudo indefinido — contato, conversa, mensagem, mídia, audit log — sem cron de expiração em lugar nenhum), processo hoje pra exclusão/portabilidade, ressalvas honestas. **Pesquisando o mecanismo real de exclusão pra não documentar algo que não funciona, achado B-28**: `ContactService#remove` fazia hard delete, mas `Conversation.contact` não tem cascade — excluir contato com histórico quebrava com violação de FK, nunca pego porque o teste mockava `prisma.contact.delete` direto e não existe consumidor no frontend. Corrigido: a rota agora **anonimiza** (`name`→"Contato removido", `phone`→placeholder único, `email`/`avatarUrl`/`metadata` zerados, novo campo `anonymizedAt`) em vez de apagar — decisão consciente (LGPD art. 16 permite reter dado anonimizado por motivo legítimo, aqui o histórico de atendimento da empresa) —, restrita a `ADMIN+` (antes qualquer autenticado podia chamar), idempotente, auditada como `contact.anonymized`. Validado contra Postgres real: contato de teste com conversa vinculada de verdade — a chamada antiga teria quebrado, a nova anonimiza com sucesso, conversa preservada, 2ª tentativa recusa com 400. Dois gaps que ficaram fora do escopo desta sessão, registrados como **B-29** (mídia no MinIO nunca é purgada, mesmo pra contato anonimizado) e **B-30** (sem endpoint de exportação/portabilidade — processo manual documentado no RUNBOOK). | B-17, B-28 (novo), B-29 (novo), B-30 (novo) | `npx prisma migrate deploy` → migration `20260725215606_contact_anonymization` aplicada · `npm test` (backend): 21 suites, **181/181** (2 novos em `contact.service.spec.ts`) · `npm run build` (nest) limpo · `curl` real (Postgres): contato com conversa real vinculada → `DELETE /contacts/:id` → 200, PII trocada, conversa intacta, 2ª chamada → 400, `GET /audit-logs?entity=Contact` confirma `contact.anonymized` com `before` correto |
| 2026-07-25 | **B-16 — 1ª execução real do CI no GitHub Actions (pedido do usuário: "vamos prosseguir para o backlog b-16").** Os workflows `api-ci.yml`/`web-ci.yml` existiam desde a Fase 7 mas nunca tinham rodado de fato no Actions — só reproduzidos localmente. **Decisão do usuário: push direto pra `master`** (em vez de branch+PR) — os 4 commits de B-15/21/B-25/B-26/B-27 já estavam prontos localmente, tocando tanto `apps/api/**` quanto `src/**`, disparando os dois workflows de uma vez pelo gatilho `push`. `git push origin master` (`46db08d..241cbf6`), confirmado via API pública do GitHub (repo é público — não precisou de `gh auth login`): **API CI e Web CI completaram com `success`**, ~50s cada, sem nenhum ajuste no runner/cache/Node 20. | B-16 | `git push origin master` → `46db08d..241cbf6` · API pública do GitHub (`/repos/Cmdev2019/atendehub/actions/runs`) confirmando os 2 runs do commit `241cbf6`: API CI (`30176186271`) e Web CI (`30176186218`), ambos `status:completed`/`conclusion:success` |
| 2026-07-25 | **B-27 — UI de tags no frontend (pedido do usuário: "vamos para o próximo backlog").** Backend pronto desde B1-1, zero consumidor no front até agora — mesma classe de bug do B-8. `apiClient` ganhou os 6 métodos de tag (CRUD + atribuir/remover em conversa, real e mock). Nova seção "Tags" em Configurações (`SUPERVISOR+`, CRUD com nome+cor+contagem de uso, exclusão via `useConfirm`). `CustomerPanel.jsx`: chips coloridos com botão de remover + seletor de "adicionar tag" (só as ainda não atribuídas), liberado a qualquer usuário autenticado. **Mudança de shape necessária:** `toUiConversation` achatava `tags` pra `string[]` (só `tag.name`) — sem o `id`, não dava pra atribuir/remover de verdade; agora preserva `{id,name,color}[]`. Validado com Playwright real (Chromium já instalado de sessão anterior) contra o front em modo mock forçado (`VITE_USE_MOCK=true`, sem tocar a API real): fila com "Entrega"/"Prioridade" já atribuídas → removeu "Entrega" (chip some) → seletor reapareceu com ela → re-adicionou (chip volta) — round-trip completo confirmado visualmente, não só por asserção de teste. Settings: criar/renomear/excluir tag testado de ponta a ponta no navegador (5→6→6 renomeada→5 tags), incluindo o modal de confirmação próprio do app (B-13) antes de excluir. Endpoints de CRUD de tag também batidos via `curl` contra o backend real (Postgres): criar, listar, excluir — todos 200/201. | B-27 | `npm test` (front): 23 suites, **183/183** (10 novos: `CustomerPanel.test.js` novo com 7, 3 em `useConversations.integration.test.js`) · `vite build` → OK · Playwright real (Chromium) dirigindo add/remove de tag na conversa e CRUD completo em Configurações, com screenshots em cada etapa · `curl` real (Postgres) criando/listando/excluindo tag via `POST/GET/DELETE /tags` |
| 2026-07-25 | **B-25 — upgrade NestJS 10→11, fechando o supply-chain de dependências de produção do backend (pedido do usuário).** `@nestjs/*` inteiro pra `^11.x` (`config`→`^4.0.4`, resolve o `lodash` vulnerável), `bcrypt` 5→6 (elimina `@mapbox/node-pre-gyp`/`tar` da árvore por completo — build nativo trocou pra `node-gyp-build`), `multer` 1→2 (sem mudança de uso — só `Express.Multer.File`/`FileInterceptor` no código). `brace-expansion` corrigido com `npm overrides` escopado por versão (só a cadeia do `@nestjs/cli`, sem mexer na do ESLint — 1ª tentativa sem escopo quebrou `lint:check`, corrigida na hora). **20 → 3 vulnerabilidades** (as 3 restantes são a mesma raiz: `uuid` via `bull`, sem fix upstream disponível, risco real baixo). Bug de tipo real encontrado e corrigido: `expiresIn` do `@nestjs/jwt` ficou mais estrito (`StringValue` da lib `ms`) e quebrou a build; corrigido com cast pro tipo público da lib, sem mudar comportamento em runtime. **Validado com infra real** (Docker Desktop religado): API subida contra Postgres/Redis reais, login real (round-trip completo de assinatura/verificação de JWT), rota autenticada (`/auth/me`, `/conversations`) e webhook (rejeita sem `apikey`, 403) todos testados via `curl` real, não só mock. | B-25 | `npm audit --omit=dev` (backend): 20 → **3** (moderadas, mesma raiz sem fix) · `npm test` (backend): 21 suites, **179/179** (baseline idêntico, sem regressão) · `npm run build` (nest) e `npm run lint:check` limpos · `curl` real: login → 200 (JWT válido) · `/auth/me` → 200 · `/conversations` → 200 · webhook sem apikey → 403 |
| 2026-07-25 | **B-26 — reset administrativo mínimo de senha (escolhido pelo usuário entre as 2 opções do item).** Novo `POST /users/:id/reset-password` (`ADMIN`-only, corpo vazio): `UserService#resetPassword` gera senha temporária de 12 caracteres já dentro da política de senha (maiúscula+minúscula+número, sem caracteres ambíguos), hasheia com bcrypt, substitui a senha do usuário-alvo, registra `user.password_reset` em auditoria e devolve `{ temporaryPassword }` em texto puro só nessa resposta (nunca logado nem persistido em claro). Bloqueia auto-reset (`id === requesterId` → 400) — limitação consciente já registrada na decisão: não resolve o `ADMIN` único da empresa se trancar sozinho, só cobre `AGENT`/`SUPERVISOR` travado com um `ADMIN` ativo por perto. Frontend: botão "Resetar senha" por usuário em `SettingsPanel.jsx` (oculto na própria linha do admin logado), confirmação via `useConfirm` (B-13), banner dismissível com a senha gerada pra repasse manual. `docs/API_CONTRACT.md` atualizado no mesmo commit. | B-26 | `npm test` back: 21 suites, **179/179** (3 novos em `user.service.spec.ts`) · `npm test` front: 22 suites, **173/173** (sem regressão) |
| 2026-07-25 | Fechados os 2 itens rápidos escolhidos pelo usuário na priorização pós-review: **B-15** ganhou `@Throttle({ limit: 200, ttl: 60_000 })` dedicado na rota de webhook (antes só herdava o limite global de 100/60s); **B-21** removeu os 2 botões decorativos sem `onClick` (lupa em `ConversationQueue.jsx`, "Mais opções" em `ChatPanel.jsx`) em vez de implementar uma ação nova para eles. | B-15, B-21 | `npm test` front 173/173 e back 176/176 (ambos verdes após as mudanças) · `npm run build` (vite e nest) limpos · suíte de webhook (41 testes) e dos 2 componentes tocados rodadas isoladamente antes da suíte completa |
| 2026-07-25 | **Review complementar pedida pelo usuário** ("review complemento e milagrosa de tudo que já foi feito... reavalie o escopo... o que passou batido... o que pode ser feito para melhorar"). Passou pelos dois roadmaps por completo, rodou as suítes reais (não confiou só na documentação) e comparou endpoints do backend com os consumidores reais do frontend. **Achado de processo (não de código):** os 49 arquivos de B-7 a B-24 estavam concluídos e validados, mas nunca tinham sido commitados — corrigido no commit `a9d9973`. **3 achados novos de produto/segurança, registrados como B-25/B-26/B-27** (nenhum corrigido ainda, ver linhas do Backlog). | B-25 (novo), B-26 (novo), B-27 (novo) | `npm test` front 173/173 (22 suites) e back 176/176 (21 suites), ambos verdes sobre o estado pré-commit · `npm run build` (vite) e `npm run build` (nest) limpos, sem warning · `npm audit --omit=dev`: front 0 vulnerabilidades, backend 20 (1 crítica/6 altas/13 moderadas) · grep em `src/services/api.js` confirmando zero chamada a endpoints de tag · leitura de `auth.controller.ts`/`user.service.ts#changePassword` confirmando ausência de reset administrativo e de infra de e-mail |
| 2026-07-25 | **Validação visual manual de B-8/B-9 + 3 bugs reais encontrados e corrigidos (pedido do usuário: "vamos fazer a validação manual pendente e prosseguir a partir desse ponto").** Docker Desktop religado, API e front reais subidos, e Playwright/Chromium **instalado com sucesso nesta sessão** (`npx playwright install chromium` funcionou de ponta a ponta — o ambiente não estava mais sem navegador, ao contrário do que B-7/B-8/B-9 registravam nas sessões anteriores). Script Playwright dirigiu o app de verdade: login real, sino de notificações (B-8, abre/fecha, estado vazio correto), logout, alternância pro cadastro e submissão do formulário de empresa nova (B-9). **3 bugs reais surgiram só com esse teste ponta a ponta** (nenhum dos 173 testes automatizados do front pegava nenhum deles): **B-22** — `useConversations.js` mantinha o fixture mock (`initialConversations`) na tela pra sempre quando o backend real respondia com fila vazia (empresa nova via B-9, ou banco sem conversas), sem nenhum banner — bypassa completamente o mecanismo oficial de mock (F2-1/F2-2); a 1ª pista foi a empresa nova recém-cadastrada mostrando a mesma conversa fixture "Marina Alves" da conta admin, o que parecia vazamento entre tenants mas era esse bug (confirmado via curl que os `companyId` são de fato diferentes e a API real retorna `[]`). **B-23** — `apiClient.logout()` não mandava `refreshToken` no corpo; o backend exige (`RefreshTokenDto`) e responde 400 (reproduzido via curl); `AuthContext#logout` engolia o erro sem limpar o usuário — clicar em "Sair" e confirmar no modal nunca voltava pro login. **B-24** — corrigir B-23 expôs que `request()` chama `response.json()` sem checar `204 No Content`, quebrando com `SyntaxError` numa resposta vazia de verdade. Os 3 corrigidos e **revalidados no mesmo navegador real** depois do fix: fila vazia mostra "Nenhuma conversa" (não mais o fixture), logout volta pro login de verdade, zero erro no console. 2 testes automatizados novos cobrindo as regressões (`useConversations.integration.test.js`, `AuthContext.test.js` — este último não existia antes). | B-8, B-9, B-22 (novo), B-23 (novo), B-24 (novo) | Chromium real via Playwright (instalado nesta sessão) dirigindo login→sino→logout→cadastro contra Docker+API+front reais, com screenshots em cada etapa · curl reproduzindo `POST /auth/logout` (400 antes do fix) e comparando `companyId`/`GET /conversations` de duas empresas reais · `npm test` (front): 22 suites, **173/173** (2 novos) · console do navegador sem erros após os 3 fixes |
| 2026-07-25 | **B-14 — acessibilidade: auditoria botão a botão + `aria-current` (pedido do usuário).** Revisão de todos os 35 `onClick` em 10 arquivos — a suspeita original de "rótulo ausente" não se confirmou em nenhum caso (todo botão só-com-ícone já tinha `title` e/ou herdava `aria-label` do `Icon`). O achado real: 3 listas com item "ativo" indicado só por CSS — `ConversationQueue.jsx` (conversa aberta), `Sidebar.jsx` (seção do menu) e `SettingsPanel.jsx` (aba de Configurações) — ganharam `aria-current` (`"true"`/`"page"` conforme o padrão de cada widget), atualizado dinamicamente conforme a seleção muda. 3 botões de excluir (conexão WhatsApp/fila/grupo) ganharam `title` — tinham nome acessível só via o ícone, sem tooltip visível pro mouse. Achado fora de escopo registrado à parte como **B-21** (2 botões decorativos sem `onClick`), não corrigido agora. | B-14, B-21 (novo) | `npm test`: 21 suites, **171/171** (6 novos: `Sidebar.test.js` +1, `ConversationQueue.test.js` novo com 2, `SettingsPanel.nav.test.js` novo com 1 — e mais os já contados) · `vite build` → OK · compilação dos 3 arquivos tocados confirmada via Vite dev server |
| 2026-07-25 | **B-13 — `alert()`/`confirm()` nativos trocados por modal próprio (pedido do usuário).** `ConfirmContext.jsx`/`useConfirm.js` novos: API imperativa `await confirm(mensagem, opções?)` — mesma ergonomia do `confirm()` nativo, resolvida por Promise em vez de bloquear a thread — renderizando um `ConfirmDialog.jsx` (modal tema-aware: overlay fecha ao clicar fora ou Escape, foca "Cancelar" por padrão, variante `danger` com botão de confirmar vermelho pra ações destrutivas). `ConfirmProvider` registrado uma vez em `main.js`. Migrados os 7 pontos: logout (`Topbar.jsx`/`Sidebar.jsx`, idênticos) + 5 dentro de `SettingsPanel.jsx` (desconectar/excluir WhatsApp, excluir usuário/fila/grupo — as 5 exclusões com `danger: true`). Nenhum dos 3 componentes tinha teste antes; `Sidebar.test.js` novo prova a integração real (não só o mecanismo isolado) — clica no botão de logout, confirma que o modal abre, e que `logout()` só é chamado depois de confirmar no modal (não no clique original). | B-13 | `npm test`: 19 suites, **167/167** (12 novos: `ConfirmDialog.test.js` 5, `ConfirmContext.test.js` 5, `Sidebar.test.js` 2) · `vite build` → OK · compilação de todos os 7 arquivos tocados confirmada via Vite dev server (200, sem erro de transform) |
| 2026-07-25 | **B-12 — Error Boundary no frontend (pedido do usuário).** `ErrorBoundary.jsx` novo (componente de classe — único jeito do React de capturar erro de render, sem equivalente em hook), envolvendo `AppContent` em `main.js` (dentro do `AuthProvider`, fora do `ErrorBoundary` só ficam os providers de contexto, que dificilmente lançam exceção de render). Fallback com ícone de aviso, mensagem e botão "Recarregar página"; `componentDidCatch` loga só mensagem/stack do erro, nunca dado do usuário. Achado no caminho (não é bug do produto, é limitação do ambiente de teste): jsdom desta versão não permite mockar `window.location.reload` (propriedade read-only, `Object.defineProperty`/assignment direto falham os dois) — resolvido tornando `onReload` uma prop injetável com esse default, em vez de testar o global do browser diretamente. | B-12 | `npm test`: 16 suites, **155/155** (4 novos) · `vite build` → OK · compilação de `main.js`/`ErrorBoundary.jsx` confirmada via Vite dev server (200, sem erro de transform) |
| 2026-07-25 | **B-11 — endpoint faltante documentado no contrato (pedido do usuário).** Nova seção `GET .../messages/:id` em `docs/API_CONTRACT.md`, escrita a partir da leitura de `message.service.ts#findOne` — documenta que a busca por id não filtra `isDeleted` (diferente da listagem, que esconde mensagem apagada) e que o `select` de `attachments` aqui é mais enxuto (sem `width`/`height`/`duration`). Item 100% documental, sem tocar em código. | B-11 | leitura direta de `message.controller.ts`/`message.service.ts` — nenhum teste roda contra documentação, então não há suíte a re-executar |
| 2026-07-25 | **B-10 — backup e restore documentados no RUNBOOK (pedido do usuário).** Nova seção cobrindo Postgres (`pg_dump -Fc` dos bancos `atendehub`+`evolution` que vivem na mesma instância, `pg_restore` com aviso de testar num banco separado antes de restaurar por cima de um vivo, lembrete de rodar `prisma migrate deploy` pós-restore) e MinIO (tar do volume Docker via container Alpine descartável, evitando reintroduzir `minio/mc` — imagem que já causou dor de cabeça em B-5 — com ressalva honesta sobre não ser um backup S3-nativo). **Validado de verdade** contra o ambiente local (Docker já estava de pé desta sessão): dump do Postgres restaurado num banco `atendehub_restore_test` separado, contagem de linhas de `users` idêntica; tarball do MinIO extraído num volume novo com a estrutura do bucket íntegra. Achado no caminho (não é bug do produto, é ressalva de documentação): comparar contagem de arquivos do volume MinIO original vs. restaurado não é teste de integridade válido contra um serviço **ao vivo** (o próprio MinIO escreve metadata em segundo plano) — documentado como nuance em vez de uma comparação enganosa. 2 bugs de ambiente Windows/Git Bash resolvidos no caminho (path mangling do `-f` do `pg_dump` e dos volumes do container Alpine) — nenhum dos dois é bug do projeto, só do jeito de rodar comandos Docker nesse shell. | B-10 | dump restaurado (`atendehub_restore_test`) → `SELECT count(*) FROM users` idêntico nos dois bancos · `tar tzf`/`tar xzf` do backup do MinIO → estrutura `atendehub-media/` íntegra · volumes/bancos de teste removidos após validação |
| 2026-07-25 | **B-20 e B-9 implementados (pedido do usuário: modelo de auto-cadastro público de empresa, "o contratante cria a empresa e o primeiro login é do admin controlador daquela empresa").** Docker Desktop religado nesta sessão especificamente pra gerar/aplicar a migration com Postgres real. **B-20 (pré-requisito):** `User.email` passou de único-por-empresa (`@@unique([companyId,email])`) para único globalmente (`@@unique([email])`) — migration `20260725120609_user_email_globally_unique` (`DROP INDEX` da constraint composta + `CREATE UNIQUE INDEX` no e-mail sozinho), aplicada via `prisma migrate deploy` (o `migrate dev` normal exige confirmação interativa que não tem como dar num ambiente não-interativo; migration escrita à mão seguindo a convenção de pasta/timestamp do Prisma e testada de verdade contra o banco). `seed.ts` ajustado (usava a chave composta `companyId_email` no upsert do admin, que deixou de existir). **Achado corrigido no caminho:** `user.service.ts#create` checava e-mail duplicado só `{companyId,email}` — com a constraint global, um e-mail de OUTRA empresa passaria pela checagem e quebraria com 500 cru no `create()`; trocado para checagem global (`findUnique({email})`), mesmo 409 de antes. **B-9:** novo `POST /auth/register-company` (público, throttle 3/60s/IP), `AuthService#registerCompany` — slug único gerado a partir do nome da empresa (normalizado, colisão com sufixo `-2`/`-3`), `Company`+`User ADMIN` numa `$transaction`, plano `FREE` por padrão (sem billing), auto-login (mesmo shape do login). Frontend: `RegisterCompanyForm.jsx` novo, `LoginPage.jsx` com alternância login↔cadastro no mesmo card, `AuthContext#registerCompany`, `apiClient.registerCompany` (real+mock), validadores novos (`companyName`, `passwordPolicy` — mesma regra do backend, evita round-trip por senha fraca). **Validado de ponta a ponta com API+Postgres reais:** cadastro com nome acentuado ("Café & Confeitaria Açúcar!!") → 201, slug `cafe-confeitaria-acucar` conferido em hex no banco (não só no terminal — a 1ª tentativa de validação via `curl -d` direto no shell mascarou o teste com mojibake do próprio terminal Windows/Git Bash, não do código; resolvido testando via arquivo UTF-8 real); e-mail duplicado → 409; dados de teste removidos do banco depois. `docs/API_CONTRACT.md` atualizado (seção Auth). **Pendência:** sem `chromium-cli`/Playwright neste ambiente, não validado visualmente num navegador real — recomenda-se esse passo antes de considerar B-9/B-8 100% fechados. | B-9, B-20 | `npm test` (backend): 21 suites, **176/176** (8 novos) · `npm test` (front): 15 suites, **151/151** (18 novos) · `tsc --noEmit` (backend) → exit 0 · `vite build` → OK · `npx prisma migrate deploy` → aplicada com sucesso · smoke real: `POST /auth/register-company` com acentos → 201 + slug correto (hex conferido) · e-mail duplicado → 409 · `SELECT encode(name::bytea,'hex')` confirmando UTF-8 real no Postgres |
| 2026-07-25 | **B-7 e B-8 implementados (pedido do usuário: "pode começar o B-7 e o B-8, mete bala").** B-7: pub/sub extraído para `EventBus` (`src/services/eventBus.js`) — `off(event, callback)` agora remove só o callback certo (antes apagava todos os listeners do evento); `on()` devolve unsubscribe; os 8 pontos que chamam `wsClient.on/off` no app migrados para handlers nomeados + `off(event, handler)` explícito (não dependem do retorno de `on()`, mantendo os mocks de teste existentes intactos). B-8: `NotificationBell.jsx` novo no Topbar (badge de não lidas, dropdown, marcar uma/todas como lidas, fecha ao clicar fora) + hook `useNotifications.js` (busca no mount, reage a `sla.breached`/`conversation.assigned` — únicos 2 eventos que criam notificação no backend, B1-3; sem socket push dedicado) + `apiClient.getNotifications/markNotificationRead/markAllNotificationsRead` (real + mock, com 2 notificações seedadas pro admin em demo). **Achado no caminho:** `websocket.js` não podia ser importado direto em teste (`import.meta.env` no escopo do módulo — mesma razão pela qual `api.test.js` já mockava `./api`); resolvido extraindo a lógica de pub/sub pura para `eventBus.js`, que não toca `import.meta` e ficou diretamente testável. **Limitação registrada:** sem `chromium-cli`/Playwright disponível neste ambiente, B-8 não foi validado visualmente num navegador real (diferente do padrão de sessões anteriores) — só por testes automatizados, build e compilação via dev server; validação manual recomendada antes de considerar 100% fechado. | B-7, B-8 | `npm test`: 13 suites, **133/133** (25 novos: `eventBus.test.js` 8, `useNotifications.test.js` 5, `NotificationBell.test.js` 8, + ajustes em `useConversations.integration.test.js` e `api.test.js`) · `vite build` → OK (321 KB / 97,7 KB gzip, sem warning novo) · `curl` em cada arquivo novo/alterado via Vite dev server → 200 (sem erro 500 de transform) |
| 2026-07-25 | **Revisão completa e independente do projeto (pedido do usuário: "review completo e meticuloso de tudo que já foi feito").** 3 auditorias em paralelo, cada uma lendo o código real em vez de confiar nos roadmaps: backend (`apps/api`), frontend (`src/`) e docs/infra/escopo geral. **Alegações de "70/70"/"31/31" confirmadas** — não é otimismo de documentação: `tsc --noEmit` limpo nos dois lados, `npm test` do backend 21 suites/168 testes e do front 10 suites/108 testes (mais do que o roadmap alegava), `SlaModule` de fato registrado, CI/Dockerfiles/RUNBOOK existem e fazem sentido, `docs/API_CONTRACT.md` incomumente fiel ao código, isolamento multi-tenant amostrado correto, limites de plano (`maxAgents`/`maxChannels`) realmente aplicados. **12 lacunas novas encontradas e registradas como B-8 a B-19** (nenhuma delas invalida um item já fechado — são pontos cegos que nenhuma fase cobria): a mais relevante é B-8 (backend de notificações pronto desde B1-3, zero consumidor no frontend) combinada com B-7 (fragilidade do pub-sub do `websocket.js`, que B-8 tornaria real pela primeira vez); B-9 (onboarding de tenant inexistente) é o maior risco de escopo para um produto que se anuncia multi-tenant. Demais itens cobrem produção (backup/restore não documentado, CI nunca validado contra o Actions real, rate limit do webhook, monitoramento de erro fora do escopo) e qualidade (Error Boundary ausente, `alert()`/`confirm()` nativos, acessibilidade, 1 endpoint fora do contrato documentado). | B-8…B-19 (novos) | 3 relatórios de auditoria independentes (backend/frontend/docs-infra) · `npx tsc --noEmit` (backend) → exit 0 · `npm test` (backend) → 21 suites, 168/168 · `npm test` (front) → 10 suites, 108/108 · grep por `TODO`/`FIXME`/`console.log`/`alert`/`aria-` em `src/` e `apps/api/src/` |
| 2026-07-24 | **B-4 — scroll infinito real na fila e no histórico de mensagens.** 100% frontend — os dois backends de paginação já existiam prontos. Fila: `loadMoreConversations()` busca a próxima página (dedupe por id contra conversas que chegaram via socket entre uma página e outra), `ConversationQueue.jsx` dispara sozinho a 150px do fim da lista (desativado com busca ativa). Histórico: `loadMoreMessages(id)` usa o id da mensagem mais antiga carregada como cursor `before`, prepend no início; `ChatPanel.jsx` dispara a 100px do topo, com cuidado pra preservar a posição visual do scroll (recalcula `scrollTop` pela diferença de `scrollHeight` antes/depois do prepend — sem isso, o efeito de auto-scroll-pro-fim existente jogaria o usuário de volta pro final toda vez que mensagens antigas chegassem). **Bug de teste (não de produção) encontrado no caminho:** um fixture de teste usava `contact` como string em vez de objeto, o que aciona um atalho de passthrough em `toUiConversation` que pula a normalização de `messages` — mascarava a conversão de `content`→`text` e quebrava a asserção; corrigido usando o shape real de contato no fixture. | B-4 | `npm test` (front): 10 suites, **108/108** (8 novos) · `vite build` → OK · smoke visual com Playwright (scroll na fila e no histórico sem erro de console novo) |
| 2026-07-24 | **B-2 — métricas agregadas de verdade.** Novo `GET /conversations/stats` (backend): contagens via `count`/`aggregate` do Prisma, direto no banco — `totalActive`, `waiting`, `open`, `resolvedToday` (indicador novo, usa `resolvedAt` do schema que nunca tinha sido exposto), `unreadCount`, `unreadConversations`. Rota declarada antes de `GET /:id` (mesma cautela de ordem já aplicada em `/users/me` na B3-1). `useConversations.js` ganhou `fetchStats()`, chamado no mount e reaproveitado dentro dos handlers de socket já existentes (sem registrar nenhum listener novo). **Achado no caminho:** `wsClient.off(event)` remove todos os listeners daquele evento, não só o do chamador — registrado como novo item de backlog (B-7) em vez de arriscar um listener próprio no `Metrics.jsx`. `Metrics.jsx` reescrito pra consumir o novo shape (5 cards, incluindo "Resolvidas hoje" que não existia). | B-2, B-7 (novo) | `npx jest` (backend): 21 suites, 168/168 (4 novos) · `npm test` (front): 9 suites, 100/100 (5 novos) · `vite build` → OK · `curl /conversations/stats` real (login real) → shape correto, sem colisão com `/:id` · Playwright: 5 cards renderizam com dado real do mock |
| 2026-07-24 | **B-5 — removido o container `minio_init` quebrado.** Serviço `minio_init` (imagem `minio/mc:RELEASE.2025-06-13T11-33-47Z`, tag que saiu do Docker Hub) removido de `docker-compose.yml` e `docker-compose.prod.yml` — era redundante mesmo antes de quebrar, já que `StorageService#onModuleInit` já faz `makeBucket`+`setBucketPolicy` no boot da API. Pitfall correspondente removido de `CLAUDE.md` (#2), `README.md` e `docs/RUNBOOK.md` (as 3 instruções de "suba nominalmente, nunca `up -d` sem listar" viraram `docker compose up -d` direto). | B-5 | volume `atendehub_minio_data` recriado do zero (sem estado anterior) + `docker compose up -d` (sem listar serviços — o comando que antes falhava) → todos os serviços sobem; log da API → `Bucket "atendehub-media" criado.` na primeira subida |
| 2026-07-24 | **Auditoria do Backlog (pedido do usuário) — B-3 e B-6 marcados concluídos.** Revisão dos 6 itens do Backlog encontrou 2 já resolvidos há sessões sem nunca terem sido fechados aqui: B-3 (notificações/auditoria — módulos `notification/`/`audit-log/` prontos desde B1-3/B1-4) e B-6 (fila `message-send` morta — removida por completo na decisão B2-1). Confirmado via leitura direta do código (`ls apps/api/src/modules/notification audit-log` existem; busca por `message-send.processor.ts` não encontra nada). B-5 confirmado ainda em aberto (`docker-compose.yml:73` ainda com a tag quebrada do MinIO). | B-3, B-6 | leitura direta do código (módulos existentes/ausentes) |
| 2026-07-24 | **🏁🏁 ROADMAP_ESTABILIZACAO.md FECHADO POR COMPLETO — 70/70 (Fases 6 e 7 concluídas: F6-4 e F7-1).** F6-4: o lado frontend já estava resolvido sem nunca ter sido fechado no roadmap — `git log -S` confirma que o `console.log` que imprimia a mensagem inteira foi trocado por um log só do `conversationId` no commit `ba110ee`, bem antes desta sessão; varredura em todo `src/` (2026-07-24) confirma zero `console.*` logando conteúdo de mensagem hoje. F7-1: novo `.github/workflows/web-ci.yml` (`npm ci` → `npm test` → `npm run build`), com `paths` restritos a arquivos do front (nunca dispara em mudança só de `apps/**`); sem lint (front nunca teve ESLint configurado, nem a dependência) nem `tsc` (JS puro). **Correção incidental:** o somatório do painel de status estava incorreto há um tempo — a soma real dos denominadores de todas as fases é 70, não 68 (provável resíduo de quando alguma fase teve a contagem de itens ajustada sem recalcular o total); corrigido agora que todas as fases fecharam e a conta ficou fácil de conferir. | F6-4, F7-1 | `npm test`: 9 suites, 98/98 (inalterado) · `vite build` → OK · sequência do `web-ci.yml` rodada manualmente e idêntica local |
| 2026-07-24 | **🏁 FASE 3 CONCLUÍDA — F3-4, badge de SLA na fila.** `useConversations.js` ganhou 3 handlers de socket novos: `sla.breached` marca `conv.slaBreached`; `conversation.assigned` (evento próprio do backend, distinto de `conversation.updated` — descoberto no caminho que já era repassado pelo `websocket.js` mas nunca escutado por ninguém no front) limpa a flag ao atribuir agente; `conversation.updated` também limpa ao sair de `WAITING` por qualquer outro caminho. `ConversationQueue.jsx` renderiza badge "⚠ SLA" + destaque visual (`.sla-breached` novo em `styles.css`). Fecha a Fase 3 inteira. | F3-4 | `npm test`: 9 suites, **98/98** (3 novos) · `vite build` → OK · validação visual real com Playwright (injeção temporária de fixture, revertida) |
| 2026-07-24 | **🏁 FASE 8 CONCLUÍDA — F8-8, filas por setor + vínculo conexão↔setor.** Nova seção "Filas de distribuição" em Configurações (ADMIN+): CRUD completo de `Queue` (nome, estratégia, tempo máx. de espera, mensagem de saudação, setor vinculado), ativar/desativar. `WhatsappSection` ganhou seletor de setor na criação de conexão, com o vínculo exibido na lista. | F8-8 | `npm test`: 9 suites, 95/95 (inalterado) · `vite build` → OK · validação real com Playwright (criar/editar/desativar fila, criar conexão com setor vinculado — tudo refletido corretamente na UI) |
| 2026-07-24 | **F8-7 — perfil próprio (nome/telefone/avatar/senha), primeiro item de frontend desde a retomada da Fase 8.** Nova seção "Meu perfil" em Configurações (visível a qualquer usuário, não só admin): edição de nome/telefone, upload de avatar (multipart, restrito a imagem) e troca de senha, todas usando endpoints já prontos do backend (B3, `ROADMAP_BACKEND.md`). `AuthContext.updateUser()` novo pra refletir a edição no Topbar sem precisar de novo login. **Achado e corrigido no caminho:** bug real no mock de `refreshToken()` (token sem id do usuário embutido, quebrando `getCurrentUser()` após o 1º refresh) — só apareceu ao validar de verdade no navegador, não em `npm test`; corrigido com teste de regressão novo. | F8-7 | `npm test`: 9 suites, **95/95** (1 novo) · `vite build` → OK · validação real com Playwright (login mock → editar perfil → upload de avatar → trocar senha, sem erro de console novo) |
| 2026-07-24 | **🏁 BACKEND FECHADO — B7 (handoff), todo o `ROADMAP_BACKEND.md` de B1 a B7 (28/31, só B3 ficou pendente antes de fechar e foi concluído na mesma sessão).** Sessão contínua fechou as Fases B5 (segurança pré-produção), B6 (backend pronto para produção), B3 (perfil de usuário) e B7 (handoff) inteiras — detalhe completo de cada item em `ROADMAP_BACKEND.md`. Refletido aqui: Fases 3/4/6/7 deste documento tiveram seus itens de backend marcados ✅ com referência ao item B-equivalente (F3-1/2/3/5→B2, F4-1..6→B4, F6-1..3/5→B5, F7-2..5→B6/B2-6); **3 itens ficam deliberadamente abertos** por serem puramente de frontend (fora do escopo backend-only da B-fase): F3-4 (badge visual de SLA na fila), metade de F6-4 (`console.log` de conteúdo de mensagem em `useConversations.js:48,64`) e metade de F7-1 (CI do front). `docs/API_CONTRACT.md` atualizado com os endpoints novos (tags/queues/notifications/audit-log já estavam de B1; adicionado agora `PATCH /users/me`, `POST /users/me/avatar`, `GET /health/ready`). Handoff (B7-2) validado com `prisma migrate reset --force` + seed do zero (autorizado pelo usuário — havia tráfego real de teste no banco de dev), `npx jest` 164/164, `tsc --noEmit` limpo, `docker compose up` nominal (postgres/redis/minio/evolution healthy), checklist de segurança da B5 revisado item a item. | F3-1, F3-2, F3-3, F3-5, F4-1..F4-6, F6-1, F6-2, F6-3, F6-5, F7-2..F7-5 | `ROADMAP_BACKEND.md` — changelog completo de 2026-07-24 (Fases B3, B5, B6, B7) |
| 2026-07-21 | **Investigação pedida pelo usuário: "erros de codificação, conversas duplicadas, checkup geral".** Codificação: **sem problema encontrado** — Postgres `server_encoding`/`client_encoding` UTF8, HTTP `Content-Type: application/json; charset=utf-8`, `index.html` com `<meta charset="utf-8">`, nenhum arquivo com BOM, zero mensagens/contatos com padrão de mojibake (`Ã©`, `â€`, `Â.`) numa varredura do banco inteiro. Integridade referencial: zero mensagens ou anexos órfãos. **Conversas duplicadas: bug real confirmado** (F0-12) — 5 contatos com 2-3 conversas `WAITING`/`OPEN` simultâneas no banco, causadas por `upsertFromWebhook` reaproveitar conversa filtrando também por `whatsappConnectionId` (toda vez que a conexão é recriada, contatos com conversa aberta ganham uma nova e a antiga fica órfã e travada). Corrigido na origem (reaproveita por contato, reaponta a conexão) + 6 conversas órfãs fechadas no banco + `GET /conversations` passou a excluir `CLOSED` por padrão (efeito colateral que fazia as fechadas continuarem visíveis). No caminho, tráfego real ao vivo expôs um bug novo (F0-13): JID `@lid` do WhatsApp (número oculto por privacidade) não era normalizado, criando contato com nome/telefone literal `"...@lid"` — corrigido com extração genérica por domínio. Observação (não é bug, registrada para a Fase 6): usuário admin acumulou 42 refresh tokens (30 ainda válidos) sem nenhuma limpeza — não fechado nesta sessão. | F0-12 ✅ · F0-13 ✅ | `SELECT` de duplicatas → 5 contatos afetados antes / 0 depois · `tsc --noEmit` → exit 0 · `curl /conversations` autenticado → 14 conversas, nomes únicos, sem duplicata · `curl /conversations?status=CLOSED` → 6 (as fechadas continuam acessíveis explicitamente) · `npx jest` (front, não afetado) → 94/94 |
| 2026-07-21 | **🏁 FASE 1 CONCLUÍDA — contrato de dados único (pedido do usuário: revisar o projeto, corrigir inconsistências e fechar a fase em andamento).** Ambiente religado do zero (Docker Desktop estava parado) — postgres/redis/minio/evolution healthy, API e front relançados em janelas próprias, `tsc --noEmit`/`jest` conferidos limpos ANTES de mexer em qualquer código (baseline 76/76). Quatro itens fechados: **F1-4** `register()` removido (código morto, rota inexistente no back, nenhuma UI chamava); **F1-3** `mockConversations.js` reescrito no shape exato do contrato (contact/agent/department/tags como objetos, enums reais, mensagens no shape de mensagem da API) — descobriu e corrigiu um bug latente no `requestMock`: `GET /conversations/:id` e `.../messages` caíam ambos em `getConversations()` (a listagem inteira), inofensivo até então só porque o mock antigo nunca disparava lazy-load; **F1-2** `Metrics` — descoberta de que o componente nunca era renderizado (import morto em `main.js`) e nunca teve CSS; reescrito para calcular indicadores reais (total/OPEN/WAITING/não lidas) a partir da lista de conversas, removendo os 2 cards sem fonte de dado real (Primeira resposta, Satisfação); **F1-6** normalizadores exportados + 11 testes novos dedicados, `Metrics.test.js` novo, `api.test.js` alinhado ao wrapper `{data,meta}`. Validado contra o backend real: `GET /conversations` com usuário logado retornou 12 conversas reais no shape exatamente esperado pelo normalizador (contact aninhado, avatarUrl do WhatsApp, tags[], _count). | F1-2 ✅ · F1-3 ✅ · F1-4 ✅ · F1-6 ✅ | `npx jest` → **94/94** (18 novos) · `vite build` → OK · `tsc --noEmit` (apps/api) → exit 0 · `curl /conversations` autenticado → 12 conversas reais, shape conferido campo a campo |
| 2026-07-17 | **Conversas novas em tempo real na fila (reportado pelo usuário como "conversas não pareadas" — era pendência do F1-2).** Diagnóstico via monitoramento: pareamento e pipeline saudáveis (mensagens no banco, respostas `DELIVERED`), mas o front não tratava `conversation.created` e o `message.new` de conversa fora da lista era descartado em silêncio — conversa de contato novo só aparecia após F5. Corrigido no `useConversations`: handler de `conversation.created` (insere no topo, com dedupe), rede de segurança no `message.new` (busca `GET /conversations/:id` para conversa desconhecida, com guard anti-corrida) e preview da fila atualizado com mensagens do cliente. | F1-2 (parcial) | `npx jest` → **76/76** (2 novos) · webhook simulado de contato novo → conversa criada no banco + eventos emitidos (dados de teste removidos após a validação) |
| 2026-07-17 | **F8-12: "Conectado" sem número pareado (reportado pelo usuário).** Diagnóstico: instância saudável na Evolution (`open`, `ownerJid` presente), mas o backend parseava o shape **v1** — `fetchInstance` nunca casava (retorno v2 é array plano) e o webhook ignorava `wuid`. Corrigido nos 3 pontos (normalização v2 no `fetchInstance` + `jidToPhone` + `wuid`/`profilePictureUrl` no CONNECTION_UPDATE); `syncStatus` passou a funcionar sem mudança por consumir o shape normalizado. Stack religada no início da sessão (Docker Desktop estava parado). | F8-12 ✅ | `GET /whatsapp/:id/status` → `{"phone":"5512996572530",...}` · banco: `phone` + `profilePicture` preenchidos · `tsc --noEmit` → exit 0 |
| 2026-07-16 | **🏁 FASE 5 CONCLUÍDA — higiene do repositório e documentação.** F5-3 decidido: frontend permanece **Vite + React** (registrado em Decisões; migração para Next.js só com necessidade concreta). F5-5: `CLAUDE.md` criado na raiz com comandos, arquitetura, convenções e os 5 pitfalls conhecidos do ambiente. README ajustado para refletir a decisão. | F5-3 ✅ · F5-5 ✅ | `CLAUDE.md` na raiz · decisão na tabela de Decisões · `npx jest` → 74/74 |
| 2026-07-16 | **About do GitHub preenchido (complemento da higiene).** gh CLI instalado via winget (autenticado com o token do Git Credential Manager); descrição do repositório definida e 8 topics adicionados (whatsapp, nestjs, react, vite, prisma, evolution-api, customer-support, multi-tenant) — estava tudo vazio. | F5-2 (complemento) | `gh repo view` → description + topics conferidos |
| 2026-07-16 | **Higiene do repositório (pedido do usuário: "informações incorretas no GitHub") — F5-1, F5-2, F5-4.** README reescrito com a realidade: a stack anunciada (Next.js 14 + TS em `apps/web`) nunca existiu — é Vite 6 + React 19 JS na raiz; roteiro obsoleto substituído por tabela de estado atual + ponteiro para este roadmap; passos de execução com os 3 pitfalls que custaram sessões de debug (chave Evolution na raiz, minio_init quebrado, senha aleatória do seed). 20 relatórios antigos (alguns afirmando mais do que o código entrega) movidos para `docs/archive/` com aviso; `LOGO_GUIDELINES.md` → `docs/`; pacote raiz renomeado para `@atendehub/web`. | F5-1 ✅ · F5-2 ✅ · F5-4 ✅ | `git mv` (histórico preservado) · raiz com apenas README + roadmap · `npx jest` → 74/74 · `vite build` OK |
| 2026-07-16 | **F1-1 concluído: contrato da API documentado em `docs/API_CONTRACT.md`.** Extraído do código real (controllers, DTOs, selects do Prisma, gateway): base URL/auth/validação/rate-limit, os 3 formatos de listagem, 9 enums, 14 recursos REST com shapes de request/response e regras de negócio (envio, permissões), contrato completo do Socket.IO e as 4 divergências conhecidas do mock. Detalhes verificados contra o código antes de afirmar (users `{data,meta}`, notes array puro, logout 204, change-password DTO). | F1-1 ✅ | `docs/API_CONTRACT.md` criado · shapes conferidos por leitura dos services |
| 2026-07-16 | **Envio de prints/anexos direto no chat (pedido do usuário — F1-11, fecha B-1).** Ctrl+V no composer cola a imagem da área de transferência com preview, sem salvar em arquivo; seletor de arquivos também envia de verdade agora (antes os anexos eram decorativos). Novo endpoint multipart `messages/media`: MinIO para o painel + base64 para a Evolution (container não resolve localhost:9000); mensagem persiste com attachment e a UI de todos os agentes recebe `message.new`+`message.updated`. Texto digitado vira caption do primeiro arquivo. | F1-11 ✅ · B-1 ✅ | `npx jest` → 74/74 · `vite build` OK · `nest build` OK · rota `POST .../messages/media` → 401 sem token (registrada+protegida) |
| 2026-07-16 | **3 bugs reportados pelo usuário no uso real, diagnosticados e corrigidos (F1-8/F1-9/F1-10).** (1) Duplicação de mensagens enviadas: corrida eco-socket × resposta REST na reconciliação otimista — só na UI, banco estava íntegro; (2) figurinhas/fotos invisíveis: mídia armazenada **criptografada** (download da URL `.enc` direta — magic bytes verificados), bucket MinIO sem leitura pública (403) e frontend sem renderização de anexos — as 3 camadas corrigidas (Evolution-first decriptado, política pública no boot, ChatPanel com imagem/áudio/vídeo/link/placeholder); (3) avatares: `fetchProfilePictureUrl` novo na Evolution + busca automática com throttle no webhook + renderização com fallback para iniciais; backfill de 5/6 contatos. Anexos corrompidos limpos. Início da sessão: usuário relatara "mensagens não chegam / não pareado" — diagnóstico: pipeline saudável, era a página do navegador com socket morto (bastou F5). | F1-8 ✅ · F1-9 ✅ · F1-10 ✅ | `npx jest` → 74/74 · `vite build` OK · `nest build` OK · attachment 404 (não mais 403) pós-policy · `UPDATE contacts` → 5 avatares |
| 2026-07-16 | **Remodelagem de ícones (pedido do usuário): emojis → SVGs do svgrepo.com.** Novo `src/components/icons.jsx` com componente `Icon` e 34 ícones inline da coleção Tabler Icons (svgrepo, MIT — 24×24, stroke 2, `currentColor`, tema-aware por herança de cor); substituição completa em Sidebar, Topbar, ChatPanel, ConversationQueue, CustomerPanel, SettingsPanel, LoginForm e DemoBanner; CSS de alinhamento ícone+texto (inline-flex + gap). svgrepo.com estava com rate-limit (429) para clientes não-navegador — SVGs baixados da fonte canônica da coleção; são os mesmos arquivos. Convenção registrada em Decisões e na memória do assistente. | F8-11 ✅ | `npx jest` → 74/74 · `vite build` → OK · varredura: zero emojis restantes nos componentes |
| 2026-07-16 | **🏁 FASE 2 CONCLUÍDA — modo demo explícito e resiliência.** (1) F2-1: fallback mock restrito a dev; em produção `ApiError` 503 visível + retry, nunca dados fictícios; (2) F2-2: banner persistente "🧪 Modo demonstração" (novo `DemoBanner`, cobre login e dashboard, some quando o backend volta); (3) F2-3: fim da race do health check — `backendAvailable` começa `null`, requests aguardam o 1º teste, revalidação com backoff 5s→30s e redetecção automática; (4) F2-4: socket sempre reconecta com token fresco (`auth` como função) + refresh automático em `connect_error` por token; (5) F2-5: `user:typing` removido (decisão registrada); (6) F1-5 de carona: classe `ApiError` substitui o objeto literal. Stack religada no início da sessão (Docker + API em janela "AtendeHub API" + front em janela "AtendeHub Front"). | F2-1…F2-5 ✅ · F1-5 ✅ | `npx jest` → **74/74** (3 novos do DemoBanner) · `vite build` → OK |
| 2026-07-16 | **🏁 FASE 0 CONCLUÍDA — round-trip real completo, validado pelo usuário na madrugada.** Mensagens de cliente chegaram em tempo real no painel e as respostas enviadas pela tela foram entregues no celular: banco mostra sequência `CLIENT`/`AGENT` com status `DELIVERED` entre 01:43 e 02:05. Depois do teste o WhatsApp deslogou a sessão (02:06, `401 Log out instance` na Evolution — esperado após uso intenso/reinícios); conexão "teste" ficou `DISCONNECTED` → re-parear via QR antes do próximo teste real. As correções da sessão anterior (F0-10, F0-11, F1-7) foram commitadas em 4 commits. | F0-4 ✅ · F0-6 ✅ | `SELECT messages ORDER BY sentAt DESC` → respostas AGENT `DELIVERED` ("então meio que zicou o cinema", 02:05) · commits `bacb1e1`…`fe331d5` |
| 2026-07-15 | **Round-trip real em andamento: recepção em tempo real FUNCIONANDO (validada pelo usuário — "as conversas começaram a aparecer").** Sessão re-pareada após reinício da máquina (conexão nova "teste"; a anterior foi invalidada pelo WhatsApp — comportamento esperado). Dois bugs descobertos e corrigidos no caminho: **F0-10** (webhooks 413 — mensagens se perdiam) e **F0-11** (nomes de contato corrompidos por JID com sufixo de dispositivo + pushName de mensagens fromMe). Contatos duplicados de teste removidos do banco. **Falta:** usuário confirmar nomes corretos após as correções + resposta enviada pelo painel chegando no celular (fecha F0-4/F0-6). | F0-10 ✅ · F0-11 ✅ · F0-6 (parcial) | Payload de teste 200KB no webhook → HTTP 200 · conversas reais criadas em tempo real (6 no banco) · `SELECT contacts` pós-limpeza → 3 contatos, sem duplicatas |
| 2026-07-15 | **Configurações funcionais em modo mock (erro "Endpoint não implementado no mock" reportado pelo usuário).** Causa imediata: a stack local inteira estava fora do ar (Docker Desktop parado — provável reinício da máquina; a janela "AtendeHub API" da sessão anterior não sobreviveu) → o front caiu no fallback mock → todas as seções de Configurações quebravam, pois o mock só conhecia auth/conversas. Dois consertos: (1) stack religada — Docker Desktop + containers + API relançada em janela própria minimizada "AtendeHub API"; (2) mock estendido com users/departments/whatsapp em memória (incl. QR de demonstração e simulação de pareamento) e `requestMock` reescrito com roteamento por path+método. É a 2ª vez que o fallback silencioso confunde o usuário (cf. incidente do login) — reforça F2-1/F2-2. | F2-8 ✅ | `npx jest` → 71/71 · health pós-restart → 200 `{status:"ok"}` |
| 2026-07-15 | **🏆 WHATSAPP REAL PAREADO PELO USUÁRIO.** Conexão "carlos teste" criada pela tela de Configurações, QR escaneado com o celular, status `CONNECTED` persistido (webhook `CONNECTION_UPDATE` aceito — prova viva do fix F8-10) e confirmado via sync com a Evolution. O aviso "não é possível conectar novos dispositivos" no celular era rate-limit temporário do WhatsApp (múltiplas tentativas); a versão Baileys estava atual (2.3000.1035194821 = master oficial). Limpeza: conexão-fixture `e2e-session` excluída (pelo próprio usuário, via UI). **Falta para fechar F0-4/F0-6:** round-trip de mensagem real (receber msg de outro número → responder pelo painel). | F8-4 ✅ | `GET /whatsapp` → status CONNECTED · `GET /whatsapp/:id/status` (sync Evolution) → CONNECTED |
| 2026-07-15 | **QR Code do WhatsApp destravado (erro "Instância e2e-session não encontrada" reportado pelo usuário).** Cadeia de 3 causas corrigidas: (1) a fixture `e2e-session` existia só no banco → instância criada na Evolution; (2) chave da Evolution desalinhada (F8-9) → `.env` da raiz criado e container recriado; (3) token de instância ≠ chave global quebraria webhooks reais pós-pareamento (F8-10) → `createInstance` passa `token` global + instância recriada + API reiniciada com build novo. | F8-9 ✅ · F8-10 ✅ · F8-4 (destravado) | `POST /instance/create` → 201 com token global · `GET /whatsapp/:id/qrcode` via API oficial → `{"qrCode":"data:image/png;base64,..."}` · health 200 pós-restart |
| 2026-07-15 | **Layout das Configurações corrigido (feedback do usuário: "espaçamentos muito pequenos").** Causa raiz: o `SettingsPanel` era renderizado dentro do grid do `.workspace` (colunas 300px/1fr/340px do chat) e ficava espremido na coluna de 300px. Criada a variante `.workspace-settings` (flex, largura total) + aumento geral de respiro: padding do conteúdo 24→32/40px, nav 240→280px, itens 12/16→16/20px, fontes e gaps maiores. | F8-3 (refinamento) | `vite build` OK · `npx jest` → 71/71 |
| 2026-07-15 | **Correção do login (incidente + pedido do usuário).** A API (processo em background da sessão) foi derrubada → o front caiu no mock silenciosamente → mock rejeitou as credenciais reais com "Senha incorreta". Corrigido: mensagem genérica "Usuário ou senha incorreta." no backend e no mock (anti-enumeração), senha do admin realinhada (`admin@demo.com` / `Admin@123` = README = mock), e API relançada em **janela própria do Windows** (minimizada, título "AtendeHub API") — sobrevive ao fim desta sessão. | F2-7 | `curl login` correto → 200 · errado → 401 `"Usuário ou senha incorreta."` · health → 200 · `npx jest` → 71/71 |
| 2026-07-15 | **Fase 8 implementada (pedido do usuário): menu reorganizado + área de Configurações.** Sidebar sem o canal WhatsApp/botão Conectar (morto) e itens sem tela ficam desabilitados; toggle de tema saiu da Topbar; novo `SettingsPanel` com 4 seções (Aparência, Conexões WhatsApp com QR + status ao vivo, Usuários com níveis Atendente/Líder de setor/Administrador, Grupos com gestão de membros); 16 novos métodos no `apiClient` (users/departments/whatsapp); visibilidade por role; view switch no Dashboard; ~330 linhas de CSS tema-aware. | F8-1…F8-6 | `npx jest` → 71/71 · `vite build` → OK · endpoints reais: `/users`, `/departments`, `/whatsapp` (+`/qrcode`, `/status`, `/disconnect`) |
| 2026-07-15 | **Refinamentos pós-smoke (observando o usuário em tempo real).** (1) `join:conversation` gated por `loadedFromApi` — reloads às 14:10-14:11 entram direto na sala real, zero WARNs de `conv-1`; (2) aviso visível de falha de envio (`sendError` + `.send-error` no ChatPanel) — antes o usuário tentou enviar 3× sem nenhum feedback na tela; (3) Evolution confirmada respondendo: erros de envio agora são `404` limpos (instância inexistente — pareamento pendente) em vez de connection refused. | F2-6 ✅, F0-4 (evidência) | Monitor do log: joins sem WARN após 14:10:15 · `ERROR EvolutionService ... 404` (14:09) · `npx jest` → 71/71 |
| 2026-07-15 | **Smoke test com o usuário real (Carlos) logado pelo navegador.** Servidor: sessão admin autenticada no socket `/ws`; página carregou as conversas REAIS e entrou na sala da conversa E2E (`join:conversation` com validação multi-tenant OK — inclusive rejeitando corretamente o id mock `conv-1` durante a transição); tentativa de envio do usuário passou todo o backend e chegou à Evolution. Normalização mínima aplicada no front para viabilizar dados reais na UI (início do F1-2): `toUiConversation`, mensagens sob demanda via `GET /conversations/:id/messages`. **Descobertas corrigidas:** Evolution em crash-loop desde sempre (banco compartilhado com a API → colisão `_prisma_migrations`) → banco `evolution` dedicado + container órfão removido → **Evolution respondendo 200 pela 1ª vez**. Incidente de ferramenta (não é bug do projeto): vite duplicado do assistente ocupou a porta 3001 por ~10min e interferiu nas chamadas REST do navegador — resolvido matando o processo. | F0-6 (parcial), F1-2 (parcial), F0-9 | Log gateway: `Cliente conectado ... user cmrm93kka` + `entrou na sala conversation:cmrm9bgs...` (13:36) · Envio: `ERROR [SendMessageService] Falha ao enviar` após passar DTO/permissões (13:51) · `curl http://localhost:8080` → 200 `"Welcome to the Evolution API"` v2.3.4 · `npx jest` → 71/71 |
| 2026-07-15 | **Fase 0 implementada e validada por E2E.** Backend: novo `HealthModule` (`GET /api/v1/health` público, sem throttle); eventos Socket.IO emitidos em **união de salas** (fim da entrega duplicada); `tsconfig.build.json` (F0-8); seed corrigido (F0-7). Frontend: socket conecta no namespace `/ws`; eventos renomeados para o contrato real (`message.new`, `conversation.updated`, etc.); normalização `toUiMessage` (CLIENT→customer); envio sempre via REST com `{ type, content }`; reconciliação da mensagem otimista + dedupe por id; `join/leave:conversation` ao trocar de conversa ativa; caminho morto `send:message` removido. Infra local: Docker iniciado, Postgres/Redis/Evolution no ar, migrations + seed aplicados, fixtures E2E criadas (`e2e@demo.com`, conexão `e2e-session`). | F0-1…F0-8 | Health: `curl` → 200 `{status:"ok"}` · E2E: login 200 c/ JWT real → socket `connected` em `/ws` (salas `company:` e `agent:`) → webhook `MESSAGES_UPSERT` 200 → `conversation.created` + `message.new` recebidos em tempo real → `join:conversation` confirmado no log do gateway · Envio REST passou DTO/permissões e chegou à Evolution (400 só por não haver sessão WhatsApp pareada) · `npx jest` → **71/71** · `tsc --noEmit` → OK · `vite build` → OK |
| 2026-07-15 | Análise completa do projeto e criação deste roadmap. Estado inicial verificado: backend compila limpo (`tsc --noEmit` OK), frontend com 66/66 testes passando, integração front↔back quebrada (health inexistente, namespace `/ws` divergente, eventos com nomes diferentes, envio de mensagem sem handler e com DTO incompatível), SLA como código morto, zero testes no backend. | — (baseline) | `npx jest` → 66 passed · `npx tsc --noEmit` → exit 0 · greps registrados na análise |
